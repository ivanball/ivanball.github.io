/* ============================================================================
   ADR card copy for the Platform page.

   The LIST itself is not maintained here: tools/build-docs.mjs enumerates
   docs-src/adr/*.md, so a new ADR appears on the page (and in the count) the
   next time the docs are built, with no edit to this file or to platform.html.

   What lives here is the short, hand-written card copy, keyed by ADR number.
   An ADR with no entry falls back to its own H1 title and first paragraph, so
   this file is a quality layer, never a gate. Adding an entry is optional and
   is how you replace a generated fallback with better copy.
   ============================================================================ */

window.ADR_CARDS = {
  "001": { title: "Manual DTO mapping", summary: "Per-entity source-generated mappers chosen over reflection-based AutoMapper." },
  "002": { title: "Navigation populators", summary: "Cross-container and cross-source eager loading via an explicit populator contract." },
  "003": { title: "Outbox dual dispatch", summary: "Outbox plus in-process dispatch plus a background processor for at-least-once delivery." },
  "004": { title: "Cross-service token validation (JWKS)", summary: "Extracted services validate RS256 tokens via JWKS / OIDC discovery, no shared key." },
  "005": { title: "Soft-delete vs. erasure", summary: "Soft-delete for lifecycle; anonymization plus outbox purge for GDPR/CCPA erasure." },
  "006": { title: "Database per service", summary: "Each service owns its DB and outbox; one context class, one instance per database." },
  "007": { title: "gRPC cross-service calls", summary: "Shared contracts, typed clients, and Result-over-the-wire for synchronous calls." },
  "008": { title: "Monolith to services plus gateway", summary: "One service host per module behind a YARP gateway; transport at the edge keeps it reversible." },
  "009": { title: "Resilience and recovery objectives", summary: "A standard resilience handler on every outbound client; declared RTO/RPO and drilled restore." },
  "010": { title: "Integration-event schema versioning", summary: "Every event carries a SchemaVersion; breaking changes use a new event type plus upcaster." },
  "011": { title: "Single-locale by design", summary: "Superseded by ADR-027: en-US only was a deliberate, revisitable non-goal." },
  "012": { title: "gRPC-host transport convention", summary: "Two coherent Kestrel profiles; the choice forces the gateway-forward mode and JWKS routing." },
  "013": { title: "Result pattern over exceptions", summary: "Expected failures are Result values; only the edge maps to HTTP or gRPC status." },
  "014": { title: "CQRS decorator pipeline", summary: "Thin handlers behind a Scrutor decorator chain whose order is load-bearing." },
  "015": { title: "Architecture fitness functions", summary: "Invariants gate the build twice: a compile-time layer guard plus a shared NetArchTest library." },
  "016": { title: "Lockstep versioning + MassTransit pin", summary: "All packages release at one version; the MassTransit v8 pin is a build gate." },
  "017": { title: "HTTP request idempotency", summary: "An attribute dedups client retries via an Idempotency-Key header and cached replay." },
  "018": { title: "Polyglot persistence", summary: "SQL Server, Cosmos, and SQLite behind one model; engine is an attribute on the config." },
  "019": { title: "Layered rate limiting", summary: "An always-on global limiter caps authenticated callers per user and exempts infra traffic." },
  "020": { title: "Permission-based authorization", summary: "An opt-in capability layer over RBAC; permission policies resolve on demand from a registry." },
  "021": { title: "Consumer-side inbox idempotency", summary: "An opt-in inbox dedups broker redeliveries by message id in the consumer's own database." },
  "022": { title: "Browser session-cookie auth", summary: "HttpOnly cookies plus an SSR-time non-validating scheme so [Authorize] passes during prerender." },
  "023": { title: "Security-response headers plus CSP", summary: "Hardened security-headers middleware with a pluggable CSP that cannot break Blazor." },
  "024": { title: "Two-channel user notifications", summary: "One use case writes a durable inbox and fires a transient SignalR push; transport is pluggable." },
  "025": { title: "Startup warm-up plus readiness gating", summary: "Warm-up tasks run at startup and a readiness gate holds probes off a warming replica." },
  "026": { title: "Two-tier caching", summary: "One swappable cache substrate (in-memory or Redis) plus an HTTP output-cache edge for public reads." },
  "027": { title: "Multi-locale i18n (supersedes 011)", summary: "English and Spanish via resource files; backend errors localized at the edge by error code, one culture cookie across SSR, Server, and WASM." },
  "028": { title: "Day / dark theme mode", summary: "A persisted light/dark toggle bound through MudThemeProvider, defaulting to the OS preference with a no-flash cookie bootstrap." },
  "029": { title: "Brute-force login protection", summary: "Email-keyed login lockout with exponential backoff plus a per-IP registration cap, covering the anonymous surface the rate limiter exempts." },
  "030": { title: "Startup sole-migrator", summary: "Each service applies its own EF migrations at boot and is the sole migrator; no deploy-step backstop." },
  "031": { title: "Feature-flag management", summary: "One flag name enforced on two surfaces: a controller gate and the outermost CQRS decorator; disabled reads as 404." },
  "032": { title: "Password hashing with legacy migration", summary: "PBKDF2 with 600k iterations for new passwords; legacy records still verify and migrate on the owner's next password set." },
  "033": { title: "Resource-ownership authorization", summary: "A row-level ownership axis beside RBAC: an owner-or-admin filter rejects mismatches and a specification row-scopes queries." },
  "034": { title: "Generic entity controllers", summary: "Every entity inherits a REST surface plus a bounded query contract: sparse fields, typed filters, sort, and pagination." },
  "035": { title: "Optimistic concurrency (RowVersion)", summary: "A RowVersion token round-trips through DTOs so a stale write surfaces as HTTP 409, gated by a fitness rule." },
  "036": { title: "External OAuth login", summary: "Google and GitHub sign-in swap a single-use code for local JWTs; provider tokens never ride the redirect URL." },
  "037": { title: "Field-level encryption at rest", summary: "An EF converter encrypts string columns with AES-256-GCM; shipped and tested, not yet wired to an entity." },
  "038": { title: "Supply-chain provenance", summary: "An SBOM release gate, committed lock files, a transitive vulnerability audit, and package sources pinned to nuget.org." },
  "039": { title: "Live channel push", summary: "Ephemeral channel events ride the existing notification hub, so one WebSocket carries durable notifications and lossy live events." },
  "040": { title: "Authenticated output caching", summary: "Public, user-independent GET endpoints cache even when requests carry a Bearer token; identity-dependent payloads never qualify." },
  "041": { title: "Observability and telemetry", summary: "A shared OpenTelemetry baseline plus CQRS duration metrics, correlation IDs, head sampling, and outbox-poll span filtering." },
  "042": { title: "Device capability abstraction (MAUI)", summary: "Per-capability contracts with browser fallbacks; the MAUI package overrides them for native heads (the fifteenth package)." },
  "043": { title: "Mobile deep links and OAuth callback", summary: "Allow-listed custom-scheme OAuth completion for MAUI plus app-association files served by each app's web host." },
  "044": { title: "Native push delivery", summary: "A third notification channel: OS-level FCM/APNs push via Azure Notification Hubs reaches backgrounded and killed apps; the inbox stays the source of truth." },
  "045": { title: "Managed file storage and avatars", summary: "Pluggable blob storage plus an image processor that strips all metadata and re-encodes uploads; avatars land as 256x256 JPEGs in a public-read container." },
  "046": { title: "HTTP API versioning strategy", summary: "Header-based versioning wired in one call; supported and deprecated versions are reported on every response, with a fitness contract asserting the headers." },
  "047": { title: "Soft-deleted-user session revocation", summary: "A middleware returns 401 for authenticated callers whose account is soft-deleted, bounding the stateless-JWT revocation window to a 30-second cache instead of the token lifetime." },
  "048": { title: "Primitive identifier type aliases", summary: "Entity IDs stay primitives behind per-module type aliases, chosen over strongly-typed ID structs: readable signatures with zero EF, serializer, or OpenAPI friction." },
  "049": { title: "Library-scoped ConfigureAwait policy", summary: "Framework packages await with ConfigureAwait(false), enforced by CA2007 at error severity; application repos keep the analyzer off because ASP.NET Core has no synchronization context." },
  "050": { title: "JWT + single rotating refresh token", summary: "A short-lived stateless JWT plus one server-stored, opaque refresh token per user that rotates on every use; each rotation slides a fixed inactivity window, and a mismatch revokes the chain." },
  "051": { title: "Client-side auth token lifecycle across render modes", summary: "One ITokenRefresher abstraction with head-specific strategies: browser heads refresh through the same-origin proxy cookie, MAUI refreshes directly and persists the rotated pair in OS SecureStorage." },
  "052": { title: "Background job execution", summary: "Work that outlives a request runs as a bounded channel plus a single-reader hosted drain, never an untracked task, so the host can cancel and await it on shutdown." },
  "053": { title: "Dual-registry package publishing", summary: "Every release pushes the same packages to nuget.org and GitHub Packages from one tag, authenticated by a short-lived OIDC exchange rather than a stored key; nuget.org is the documented install path because the GitHub registry needs a token even for public packages." },
  "054": { title: "Saga compensation + reconciliation backstop", summary: "Cross-boundary consistency without two-phase commit: each step raises a domain event and its compensating action runs in its own scope, made idempotent by a marker committed alongside the compensating writes, with a periodic sweep as the saga-timeout backstop." },
  "055": { title: "Repository + Specification data-access contract", summary: "The read contract splits into id lookups and collection queries, with the raw IQueryable surfaces confined to the composite, because a raw-queryable handler is EF-coupled and cannot move behind a gRPC boundary later." },
  "056": { title: "Blazor render-mode strategy", summary: "InteractiveAuto is declared once on the root router with prerendering left on, and the resulting double fetch is cancelled once in the shared list-page base by persisting the prerendered payload rather than by turning prerendering off." },
  "057": { title: "Expand/contract schema evolution as a CI gate", summary: "A migration added by a PR cannot drop a column, table, or index without an explicit override marker, because deploy rollback is revision-only and never reverts schema: the previous release has to keep running against the new one." },
  "058": { title: "Runtime conformance suites shipped as a package", summary: "Abstract contract bases that a host subclasses to prove it wired the framework's runtime contracts, asserted against a really booted host rather than by reflection over registrations. The runtime half of the fitness-function story." },
  "059": { title: "The IModule contract and module composition", summary: "Modules are discovered by reflection and registered in topological dependency order, and a disabled module is represented by stub registrations rather than by absence: the mechanism that lets one module run as its own service." },
  "060": { title: "Performance-regression gate", summary: "A committed benchmark baseline checked in CI, gating allocations absolutely but latency only as a ratio between benchmarks, which is what keeps the gate meaningful on shared, noisy runners. A missing measurement fails rather than passes." },
  "061": { title: "Runtime secret management via Key Vault references", summary: "Every production secret lives in Azure Key Vault and reaches the app as a reference resolved by one shared user-assigned managed identity: no container app carries an inline value, the same identity pulls from the registry, and SQL authentication is staged behind a flag toward the same model." },
  "062": { title: "SLO alerting as code with an alert-to-runbook gate", summary: "Each consumer declares its SLO alerts as data in its Bicep template, materialized as Log Analytics scheduled query rules whose predicates exclude the routine 401s and hub-connection lifetimes that used to page, with a framework build gate failing when an alert and its runbook section fall out of step." },
  "063": { title: "WCAG 2.1 AA as a shipped test contract", summary: "Accessibility conformance as a named, versioned contract in the E2E package: one pinned axe tag list with advisory rules deliberately outside it, a violation thrown as a test failure pointing at the element, inherited by consumers through the workflow bases and wired as a merge check and deploy gate." },
  "064": { title: "Deploy preconditions as proof-of-recency gates", summary: "A production deploy is blocked not only on green tests but on proof that the out-of-band verification still means something: three gates assert a real DR drill, load run, and broker round-trip happened recently, because a scheduled workflow can fail silently for weeks behind a green-looking history." },
  "065": { title: "Scaffolding templates derived from the reference app", summary: "A dotnet new template pack (mmca-app, mmca-module, mmca-command, mmca-query) generated from the minimal seed, replacing the 12 projects, 105 files, and 6,596 hand-created lines that stood between the getting-started guide and the first line of business logic." },
  "066": { title: "Broker transport selection and dev/prod parity", summary: "One IMessageBus abstraction with a three-value transport selector chosen at the deployment edge, never in application code: the AppHost wires RabbitMQ locally, Bicep injects Azure Service Bus in production, both configured identically, and a dedicated test tier exercises the transport only production uses." },
  "067": { title: "Shared Blazor shell + IUIModule composition", summary: "The application shell (router, layout, nav, and the auth and notification pages every app needs) ships in the framework package, and each module contributes pages and navigation by implementing IUIModule, resolved from DI so nothing in the shell names a module: ADR-059's composition model applied to the UI layer." },
  "068": { title: "Value objects as validated domain primitives", summary: "A domain value that carries an invariant is an immutable record value object with a private constructor and a Result-returning factory, while identifiers stay primitives (ADR-048): the asymmetry is the decision, and the factory shape is fitness-enforced rather than conventional." },
  "069": { title: "Shared DataProtection key ring for scaled-out hosts", summary: "One opt-in registration persists the DataProtection key ring to a single Azure blob so every replica of a scaled-out host can decrypt the cookies and antiforgery tokens its peers mint; an absent configuration key means the single-process in-memory default stays, with no Azure dependency at startup." },
  "070": { title: "Fail-fast configuration contract", summary: "Every settings section binds through data-annotation validation with ValidateOnStart, so a missing or unparseable value refuses to boot the host instead of surfacing as a 500 at first use, and a setting read above Infrastructure goes through a read-only interface rather than the options pipeline." },
  "071": { title: "Barcode scanning and QR display capability", summary: "One feature split by what each half actually depends on: rendering a QR needs no device, so it ships as a plain shared component, while reading one needs a camera and ships as a capability contract with a null fallback and an opt-in native implementation, because a camera costs an app-store permission declaration a head may not want." },
  "072": { title: "QR badge check-in and points gamification", summary: "The badge QR carries an opaque server-verified handle rather than a signed token, organizers scan attendees rather than the reverse, and one unique index serves as both the redelivery-idempotency guard and the anti-farming rule for an append-only points ledger." },
  "073": { title: "Multi-tenancy: shared schema plus DB-per-tenant", summary: "A second named query filter composes by AND with the existing soft-delete filter and reads the tenant live from the executing context, so one cached model per source serves every tenant with the tenant id as a parameter; a write interceptor refuses cross-tenant writes, and a per-tenant connection-string override buys database isolation without a second model." },
  "074": { title: "Recurring job scheduler", summary: "Durable cron jobs built on the outbox's own claim-lease idiom rather than on Hangfire or Quartz, because the framework already owns a lease-based, multi-replica-safe polling loop in production; a missed schedule runs once and then advances, so an outage cannot produce a catch-up storm." },
  "075": { title: "Audit trail: field-level change history", summary: "A fourth save-changes interceptor diffs the change tracker and writes per-property history rows in the same transaction as the data, opted in per entity by a marker; personal data is redacted at capture rather than at read, because a trail outlives the row it describes." },
  "076": { title: "Data-subject export (DSAR) contract", summary: "The orchestration every export shares (ownership gate, aggregate load, section fan-out, envelope) moves into the framework while the app-specific projections stay app-specific as registered sections; a failing section degrades to an unavailable flag rather than failing the package, because a subject request is a legal deadline." },
  "077": { title: "HybridCache as an opt-in cache substrate", summary: "An L1-plus-L2 implementation joins the two auto-selected cache stores, opted into explicitly, and writes under a disjoint keyspace so two serialization formats can never share one key: the structural fix for the class of bug that once produced a production WRONGTYPE, including mid-rolling-deploy." },
  "078": { title: "CSV export as a dedicated endpoint", summary: "Export ships as its own route rather than as content negotiation, because the output-cache policy does not vary by Accept and a negotiation failure silently returns JSON; the endpoint page-loops past the pipeline's unpaged row ceiling and streams RFC 4180 rows written in-house, with truncation signalled by a header." },
  "079": { title: "Shared HTTP middleware pipeline", summary: "One framework method fixes the middleware order for every REST/gRPC host, with the load-bearing adjacencies (authentication before the rate limiter and before tenant resolution) commented where they sit; hosts extend by appending after the call, and conditional middleware registers unconditionally but stays inert by config." },
  "080": { title: "Rollout with automatic revision rollback", summary: "Every production deploy ends in a smoke gate asserting expected status codes; on failure each container app walks back to its previous revision, and a rollback that itself fails escalates loudly as a fleet split. Schema is never rolled back, which is the premise the expand/contract gate rests on." },
  "081": { title: "Cost baseline as a deploy gate", summary: "A read-only workflow asserts the production footprint still matches its declared cost baseline (replica ceilings, SQL tiers) and sits in deploy.needs, so an un-reverted manual surge blocks the next deploy instead of quietly billing forever." },
  "082": { title: "Two-tier cross-origin posture", summary: "Service hosts run named allow-listed CORS policies selected per environment inside the shared pipeline; the gateways run a default policy that restricts only origins, because a reverse proxy has to pass arbitrary client headers through." },
  "083": { title: "CRUD lifecycle event taxonomy", summary: "One entity-changed event with a state discriminator replaces per-entity Created/Updated/Deleted triples, and handlers filter on the state; business state-machine transitions keep their own event types, and the discriminator rides integration events as a frozen wire field." },
  "084": { title: "Stripe webhook ingress contract", summary: "An anonymous signature-verified endpoint whose status code means accepted-not-processed, because rejections make the provider disable the endpoint; a startup service self-registers the webhook and logs its freshly minted signing secret loudly for an operator to persist." },
};
