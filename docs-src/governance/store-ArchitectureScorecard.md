# MMCA.Store: Architecture Scorecard

> **Canonical, version-controlled scorecard for this repo**: the single source of truth for MMCA.Store's architecture scores. Scored against the rubric at [`ArchitectureEvaluationCriteria.md`](ArchitectureEvaluationCriteria.md); framework-wide facts in [`../MMCA.Common/FACTS.md`](https://github.com/ivanball/MMCA.Common/blob/main/FACTS.md). Remediation lives in [`RemediationBacklog.md`](../governance/store-RemediationBacklog.md); the cross-repo comparison in the workspace-internal `Docs/Architecture/CrossRepoComparison.md` (not published).

_Evidence as of 2026-09-20 at MMCA.Common.* v1.206.0 (all 17 packages, lockstep), git HEAD f983cc08 (clean tree). Rubric version 2._

## At a glance

- **Maturity index** = Σ(maturity×weight) ÷ Σ(weight×4) = 311 ÷ 316 = **98.4%**
- **Implementation index** = Σ(impl×weight) ÷ Σ(weight×10) = 665 ÷ 790 = **84.2%**
- **Weaker axis:** Implementation (execution quality), by ~14 points, a wider gap than ADC's.
- **Below maturity 4:** 2 categories (§12 Performance, §21 Accessibility); see the [maturity band](../governance/store-RemediationBacklog.md#maturity-band).
- **Implementation band (implementation <= 8):** 20 categories; see the [implementation band](../governance/store-RemediationBacklog.md#implementation-band).

MMCA.Store is a .NET 10.0 DDD/Clean Architecture e-commerce system (Catalog, Sales, Identity modules; Stripe checkout) running as independently hosted services behind a YARP gateway, database-per-service (`Store_Catalog`/`Store_Sales`/`Store_Identity`, each with its own outbox), collaborating via Result-over-the-wire gRPC and MassTransit integration events, and consuming `MMCA.Common.*` in lockstep with MMCA.ADC. Its strongest ground is inherited framework substance guarded by 44 architecture fitness-test classes, reference front-end security (§26), and an operational floor of two-phase Bicep IaC, OIDC + managed identity, a post-deploy smoke gate with auto-rollback, a drilled DR restore and freshness-gated deploys. The weight of the remaining gap sits on the implementation axis: 18 categories are at M4/I8, led by the weight-3 lockstep deploy (§7), messaging (§10), security (§11), UI architecture (§18), state management (§19) and front-end testing (§28).

## Top 5 risks

1. **Accessibility maturity is capped pending a human pass**: §21 (mat 3, weight 3): the 32-scan axe suite gates the deploy (impl 8), but the rubric pairs axe-in-CI with a recorded manual screen-reader pass and the `ACCESSIBILITY-SCREENREADER-PASS.md` results log holds only the placeholder row (`store-ACCESSIBILITY-SCREENREADER-PASS.md:70`), matching ADC's M3.
   - _Remediation:_ record a dated NVDA/VoiceOver pass in the runbook against the running app (needs a human; cannot be done headless). _Expected:_ §21 mat 3→4.
2. **Load evidence is capacity-planning cadence, not a merge gate**: §12 (mat 3): the k6 suite is real with pass/fail thresholds and a `load-freshness` deploy gate, but the test itself runs monthly/on-demand (`load-test.yml:21` cron, `workflow_dispatch` at `:12`), so a latency regression can merge and deploy inside the freshness window. `load-freshness` also carries a break-glass skip (`deploy.yml:802-809`, exit 0 at `:809`, job at `:775`) and keys on the latest successful run (`:812`).
   - _Remediation:_ right-size deliberately: either accept the monthly cadence as the recorded posture (matching ADC's accepted §12 M3) or add a cheap latency-regression smoke to the merge path. _Expected:_ decision recorded either way; §12 mat 3→4 only with a gate.
3. **Design-system residual palette**: §20 (impl 8): the inline-style sweep is at zero and pinned by `CategoryListTests.cs:127`, but the StoreHome landing stylesheet hard-codes a hex palette behind `!important` with only `--mmca-primary` tokenized (`UI.Web.Client/Pages/StoreHome.razor.css:218,289`, duplicated in the MAUI head's copy).
   - _Remediation:_ tokenize the StoreHome palette onto the theme variables and drop the `!important` overrides in both hosts, extending `BrandColorTokenTests` to the full literal set. _Expected:_ §20 impl 8→9.
4. **Client validation short of server parity**: §24 (impl 8): the four create forms' guard/dirty/validated-`MudForm` markers are CI-gated (`FormsConventionTests`, M4), but client validation is DataAnnotations-level rather than full FluentValidation parity with the server rules.
   - _Remediation:_ mirror the remaining server-only rules client-side where they do not need the DB. _Expected:_ §24 impl 8→9.
5. **Services deploy in lockstep**: §7 (impl 8, weight 3): the build half of independent deployability is per-image incremental, but the deploy half is one `azure/arm-deploy` of the whole `main.bicep` (`deploy.yml:1424`) with all five images pinned to one `IMAGE_SHA` (`:1306`) and a whole-fleet rollback (`:1566`), the rubric's "services that must deploy together" red flag.
   - _Remediation:_ split the container-app image parameters so an unchanged service keeps its current revision and the rollback iterates only the apps the deploy touched, or record the lockstep as the accepted posture for a five-app fleet. _Expected:_ §7 impl 8→9, or a Deliberate and accepted entry.

## Top 5 strengths

1. **Clean Architecture + DDD + CQRS depth, fitness-enforced**: §3/§4 (impl 9): inherited framework substance, guarded by the shared NetArchTest suite (44 test classes, `StoreArchitectureMap.cs:14-43`).
2. **Full microservices parity on a race-safe data layer**, §7 (impl 8) / §8 (impl 9): database-per-service + per-service outbox + versioned gRPC + MassTransit broker + RS256/JWKS, extractable modules (`infra/main.bicep:919-943` per-service DBs, `:965` Service Bus), with the oversell race closed by an atomic conditional UPDATE under deterministic lock ordering plus a schema CHECK backstop (`InventoryAllocationService.cs:71`, `InventoryItemConfiguration.cs:27`) and destructive migrations blocked at the merge gate (`deploy.yml:303-348`).
3. **Exemplary DevOps / operational floor**: §17 (impl 9) / §31 / §12: two-phase Bicep, OIDC + Key Vault MI, passwordless MI-SQL active in prod, post-deploy smoke gate with auto-rollback (rollback loop `deploy.yml:1566` over the app list at `:1468`), `cost-guard`/`dr-drill`/`load-test` workflows.
4. **Reference front-end security**, §26 (impl 9): in-memory access token + HttpOnly refresh cookie (no localStorage), hardened origin-pinned CSP, runtime-config fetch (`ServerTokenStorageService.cs:10-13`, `BlazorCspPolicyProvider.cs:71`).
5. **Drilled DR + real erasure path** (§29 impl 9 / §30 impl 8): recorded 28.9-min restore (`DISASTER-RECOVERY.md:186`, drill-history table `:186-189`), the `dr-freshness` deploy gate + weekly cron + CI-gated `GracefulShutdownTests`, `IAnonymizable` anonymize-in-place + export + residency/PII fitness functions.

## Scorecard

| # | Category | Weight | Maturity (0-4) | Impl (0-10) | Weighted (M·w / I·w) | Evidence / Notes |
|---|---|---|---|---|---|---|
| 1 | SOLID Principles | 3 | 4 | 9 | 12/27 | Narrow-interface ctor DI, OCP via state strategy (no type-switch); the constructor-dependency ceilings are machine-enforced by `ConstructorDependencyCountTests` (services at `:24`, controllers 8 at `:42`, handlers 7 at `:60`; `AuthenticationService` at the service high-water mark; the rubric 8+-dependency red flag is cleared), and `TimeProvider` is injected over ambient `DateTime.UtcNow`. Evidence: `Tests/Architecture/MMCA.Store.Architecture.Tests/Cqrs/ConstructorDependencyCountTests.cs:20`; `AuthenticationService.cs:26`; `OrdersController.cs:36-47`; `Order.cs:69-77` (`FrozenDictionary<OrderStatus,IOrderState>`) |
| 2 | Design Patterns | 2 | 4 | 9 | 8/18 | Factory→Result, payment State machine, Saga (Stripe compensation), Specification, Decorator, Outbox, Repository/UoW: idiomatic, named. Evidence: `Order.cs:102,69-77` (factory at `:102`, state table at `:69-77`); `Orders/Saga/OrderCancelledSagaHandler.cs`; `Orders/Specifications/OrdersByCustomerSpecification.cs` |
| 3 | Clean Architecture | 3 | 4 | 9 | 12/27 | NetArchTest-enforced layer rules; domain framework-pure. (The compile-time layer-guard MSBuild target is MMCA.Common-internal; it is not a Store-side gate.) Evidence: `StoreArchitectureMap.cs:14-43` + `LayerDependencyTests`/`DomainPurityTests`; `Order.cs:1-13` (imports only Common.Domain/Shared) |
| 4 | Domain-Driven Design | 3 | 4 | 9 | 12/27 | Aggregate root, `Money` VO, by-id cross-aggregate refs, domain events, invariants, factory→Result, rich state behavior; identifier aliases. Evidence: `Order.cs` (`Money` :37, `OrderInvariants`, `OrderIdentifierType`) |
| 5 | Vertical Slice Architecture | 2 | 4 | 8 | 8/16 | Cohesive command+handler+request+validator+mapper per operation; a deliberate layered-by-project hybrid (cross-cutting in the pipeline). Slice cohesion is machine-enforced pre-merge by `SliceCohesionTests` (sealed subclass of the shared `SliceCohesionTestsBase`, two real rule facts; the base carries no minimum-scanned-types floor, and the scan is non-vacuous in practice because `StoreArchitectureMap` anchors real Application assemblies) in the deploy-gating `MMCA.Store.CI.slnf`, the same gate ADC credits at M4/I8. The hybrid is an implementation-axis cap, not a maturity deduction. Held at 8, not 9: horizontal technical folders persist inside the module Application layers, gate-invisible where the validated type is cross-assembly (the co-location rule exempts them, `MMCA.Common .../ArchitectureRules.Slices.cs:48`; instance: `Identity.Application/Users/Validation/ChangePasswordRequestValidator.cs:10` over the Shared `ChangePasswordRequest` while its command+handler live in `Users/UseCases/ChangePassword/`), and generic-CRUD operations dispatch to shared framework handlers (`Catalog.Application/DependencyInjection.cs:43`, `Identity.Application/DependencyInjection.cs:48`) with reads served by the generic `IEntityQueryService` (`CategoriesController.cs:34`). Evidence: `Tests/Architecture/MMCA.Store.Architecture.Tests/Cqrs/SliceCohesionTests.cs:9`; `MMCA.Store.CI.slnf:53`; `Application/Orders/UseCases/Cancel/{CancelOrderCommand,CancelOrderHandler}.cs` |
| 6 | CQRS & Event-Driven | 2 | 4 | 8 | 8/16 | Command/query split, outbox persist-then-publish, idempotent inbox (`AddInboxMessages`), Stripe saga; one cross-service event flow (`ProductVariantChanged`) + a deliberate in-process `UserRegistered` domain event; the outbox-to-broker-to-consumer round-trip is covered by the nightly `MMCA.Store.CrossService.IntegrationTests` (Testcontainers RabbitMQ+SQL). Held at 8: the non-atomic publish window on the `ProductVariantChanged` Added path is narrowed by the ADR-114 internal-command adoption (`AddVariantHandler.cs:76-96`) but not closed. Evidence: `Sales.Service/Program.cs:178-179`; `Contracts/IntegrationEventContractTests.cs`; `Tests/Integration/MMCA.Store.CrossService.IntegrationTests/` |
| 7 | Microservices Readiness | 3 | 4 | 8 | 12/24 | **Database-per-service** + per-source outbox, async events + versioned gRPC, Polly resilience, extractable modules, 5 independently-built images. All per-service DBs share one physical SQL *server*, self-documented at `infra/main.bicep:908`. Maturity 4: the §7 fitness classes (`MicroserviceExtractionTests` / `ModuleIsolationTests` / `ServiceContractPurityTests` / `ProtoContractTests`; `MicroserviceExtractionTests.cs:3`) ship in `MMCA.Store.CI.slnf:53` and execute in the required `build-and-test` job (`deploy.yml:244`) and again in `backend-test-gate` (`:404`). Held at 8, not 9: "services build/test/deploy independently" is half met. The BUILD half is per-image incremental (dirty classifier `deploy.yml:138-161`, matrix `:1071-1087`, unchanged legs re-tagged instead of rebuilt `:1172`); the DEPLOY half is a single `azure/arm-deploy` of the whole `main.bicep` (`:1424`) with all five container-app images pinned to one `IMAGE_SHA` (`:1306`), so every service takes a new revision on every deploy and a smoke failure rolls all five back together (`:1566`, app list `:1468`). That is the rubric's "services that must deploy together" red flag, which keeps the row in the Strong band rather than Exemplary. A secondary rubric gap, not a downgrade basis: the Anti-Corruption Layer / Strangler Fig vocabulary has zero occurrences in the repo. Evidence: `infra/main.bicep:919-943` (per-service DBs, names `:913-917`) + `:965` (Service Bus namespace); `Sales.Service/Program.cs:246-247` (gRPC typed clients), `:257` (`AddBrokerMessaging`); `deploy.yml:874-894` (`cross-service-freshness` requires BOTH the RabbitMQ round-trip and the Service Bus emulator smoke to have succeeded in the same nightly run) |
| 8 | Data Architecture | 3 | 4 | 9 | 12/27 | Per-aggregate tx, soft-delete + filtered indexes, central audit (Common), RowVersion concurrency, versioned per-service migrations with a model-drift CI gate, LTR backups. The write path is race-safe by construction: stock decrements are an atomic conditional UPDATE (`SET qty = qty - n WHERE qty >= n`) with deterministic variant-id lock ordering, enlisted in the ambient transaction and stamping the audit columns explicitly because `ExecuteUpdate` bypasses the audit interceptor (`InventoryAllocationService.cs:71`); a `CK_InventoryItem_AvailableQuantity_NonNegative` CHECK constraint backstops it at the schema so no future path can drive stock negative (`InventoryItemConfiguration.cs:27`); the checkout write phase (decrements + order insert + cart transition) is one `ExecuteInTransactionAsync` with the cross-service gRPC price fetch deliberately outside it, so remote latency never extends lock hold time (`CheckOutHandler.cs:90`); and an expand/contract guard fails any PR whose new migration `Up()` drops a column/table/index without an `EXPAND-CONTRACT-OVERRIDE` marker, failing closed when the base diff is unresolvable (`deploy.yml:303-348`; fail-closed branch at `:332`, override marker check at `:347-348`; policy at `CONTRIBUTING.md:55`). Both migration gates sit in the required `build-and-test` check. Held at 9, not 10: Identity carries `IConcurrencyAware` mutation requests but has no concurrency round-trip test, so the 409 proof covers 2 of 3 modules; the shared `ConcurrencyConventionTests` rule scans Application types named `*UpdateRequest`, of which Store has none, so that particular gate is vacuous here (the substance stands without it). Evidence: `InventoryAllocationService.cs:71`; `InventoryItemConfiguration.cs:27`; `CheckOutHandler.cs:90`; API-level round-trip proof incl. the child-entity token path in `Tests/Integration/MMCA.Store.Catalog.IntegrationTests/Concurrency/StaleRowVersionConflictTests.cs:45` and `Tests/Integration/MMCA.Store.Sales.IntegrationTests/Concurrency/OrderTransitionConcurrencyTests.cs:23`; raw-`IQueryable` ban with an EMPTY allowlist in `Tests/Architecture/MMCA.Store.Architecture.Tests/Cqrs/RawQueryableConventionTests.cs:14` (in `MMCA.Store.CI.slnf:53`); per-module model-drift gate `deploy.yml:287`; `main.bicep:660-672` (LTR policy) |
| 9 | API & Contract Design | 2 | 4 | 9 | 8/18 | RFC 9457 Problem Details, header versioning, pagination, DTO decoupling (ADR-001), gRPC `.proto`, OpenAPI served non-prod, and a demonstrated v2 contract: `ServiceInfoController` carries `[ApiVersion("1.0", Deprecated)]` + `[ApiVersion("2.0")]`. The guards are seven contract-guard files across all three services (OpenAPI shape/path-floor + RFC 9457 Problem Details for Catalog/Sales/Identity, incl. the Store-specific 409 stale-RowVersion probe, plus ServiceInfo API-versioning on Catalog), running in `integration-tests`, which is PR-only (`deploy.yml:485-491`, the `pull_request` condition at `:491`, absent from `deploy.needs` at `:999`) but a server-side REQUIRED status check on `main` with strict=true (`CONTRIBUTING.md:83`), and merging to `main` is the prod deploy, so nothing ships without them green; `ProtoContractTests` additionally freezes the full cross-service gRPC wire contract in the merge-gating architecture tier (`ProtoContractTests.cs:9`). Below 10: OpenAPI not exposed in prod (internal behind gateway); v2 demonstrated on one anonymous diagnostic endpoint in one service (all 14 business controllers are v1.0-only). Evidence: `Catalog.API/Controllers/ServiceInfoController.cs:18`; `Tests/Integration/MMCA.Store.Catalog.IntegrationTests/Contract/{ApiVersioningTests,OpenApiContractTests,ProblemDetailsContractTests}.cs` + Sales/Identity siblings; `OrdersController.cs:46,91,120,127-129`; `Sales.Service/Program.cs:158,294,311-315` (non-prod `MapOpenApi` at `:311-315`) |
| 10 | Messaging & Integration Architecture | 3 | 4 | 8 | 12/24 | Rubric v2 category ([ADR-110](../adr/110-rubric-v2-category-realignment.md)) at the v2 default weight 3. Broker topology and transport are ADR-governed (ADR-066: RabbitMQ locally, Service Bus in production, `Sales.Service/Program.cs:256` `AddBrokerMessaging`), poison-message and dead-letter handling per ADR-087 with per-service outbox/inbox retention indexes (`MMCA.Store.Migrations.SqlServer.Sales/Migrations/20260725133726_AddOutboxInboxRetentionIndexes.cs`) and a 30-day dead-letter retention setting (`infra/main.bicep:1034`), saga compensation on order cancellation (`Orders/Saga/OrderCancelledSagaHandler.cs`), the gateway kept to routing and auth only per ADR-088 with `RouteMapTests` pinning every route, the wire contract of all four integration events frozen by `Tests/Architecture/MMCA.Store.Architecture.Tests/Contracts/IntegrationEventContractTests.cs:9-15` in the required `build-and-test` check, the inbox on in all three services, and the real broker round-trip enforced as a deploy gate: `cross-service-freshness` (`deploy.yml:834`, in `deploy.needs` at `:1200`) requires both the RabbitMQ and the Service Bus emulator tiers of `cross-service-tests.yml:104` to have succeeded recently. Cross-cutting facets (pipeline decorators, typed `ValidateOnStart` options, CORS/versioning/rate-limit/output-cache/compression, shared Polly) score in §5/§6/§9/§12/§17/§29; none is copy-pasted, and the IsFailure-guarded cache-eviction pattern ADR-001 describes is present (`Catalog.API/Controllers/ProductsController.cs:152,173`). Held at 8, not 9: three of the rubric's seven criteria stay short of the 9 bar on what happens after a message leaves the process; the lever for 9 is not yet identified. Evidence: `Sales.Service/Program.cs` |
| 11 | Security | 3 | 4 | 8 | 12/24 | RS256/JWKS (no shared secret), server-side resource authz (404-not-403), KV secrets via UAMI, EF parameterized, Stripe webhook-secret handling. Evidence: `main.bicep:786` (UAMI), `:874-913` (Key Vault + secrets), `:600-608` (Entra SQL admin); `Sales.Service/Program.cs:174-179` (JWKS-backed bearer auth); `OrdersController.cs:35,143,190,291-317` |
| 12 | Performance & Scalability | 2 | 3 | 8 | 6/16 | Async throughout, projections/AsNoTracking/paging on hot paths, tiered cache (output + Redis), stateless scale-out, a real k6 load test, client Web Vitals measured per E2E run (LCP/CLS/TTFB/FCP written as CI artifacts), one batched cross-service existence check in bulk inventory (`Sales.Application/Inventory/UseCases/BulkSet/BulkSetInventoryHandler.cs:36-41`, rationale `:30-34`), and cross-replica output-cache eviction for non-controller writes (`Catalog.Service/Program.cs:173`, broker half `:280`). Maturity 3, not 4: no perf check runs on the merge path. `load-test.yml` is a monthly cron (`:21`) plus `workflow_dispatch` (`:12`); the only deploy-chain hook, `load-freshness` (`deploy.yml:775`), is push-only (`:778`), bounds staleness rather than detecting regressions, keys on the latest SUCCESSFUL k6 run (`:812`) so a threshold failure blocks nothing until the last success ages out, and carries a break-glass skip that exits 0 with no capacity proof (`:802-809`, exit 0 at `:809`), which the deploy reads as `success` (`:1200` needs, `:1236`); the Web Vitals budgets ride an `e2e-gate` the deploy accepts as skipped (`:1241`); none of the four required merge checks is a latency gate (`CONTRIBUTING.md:83`). Held at 8: the k6 asset is anonymous, read-only and Catalog-only at 40 VUs (`Tests/Load/k6/catalog-read-load.js:3,15`). Evidence: `Tests/Load/k6/catalog-read-load.js` + `load-test.yml:12,21` (synthetic-traffic header `:6-8`, `:66`); `Tests/E2E/MMCA.Store.E2E.Tests/Workflows/WebVitalsTests.cs` + `e2e.yml:383` (`WEB_VITALS_OUTPUT_DIR`); `OrdersController.cs:102-112` (bounded pageSize `:110`, `asTracking: false`, field projection) |
| 13 | Observability & Operability | 2 | 4 | 9 | 8/18 | OTel logs/traces/RED metrics via ServiceDefaults (incl. `MMCA.Common.Outbox`), `/health`+`/alive`+`/health/ready`, App Insights + 3 SLO alerts + action group + saved SLO workbook provisioned in IaC, per-alert operations runbook in-repo, correlation, poll-span noise control, CI-gated graceful shutdown. Maturity 4: the alert-to-runbook pairing is a CI-gated fitness function: `ObservabilityConventionTests` parses the embedded `infra/main.bicep` `sloAlertSpecs` and fails the merge gate on any alert without a severity-correct `OPERATIONS.md` section (3-spec non-vacuity floor). Evidence: `Tests/Architecture/MMCA.Store.Architecture.Tests/Governance/ObservabilityConventionTests.cs` (a sealed subclass; the three [Fact]s live in `MMCA.Common.Testing.Architecture/Bases/ObservabilityConventionTestsBase.cs`); `MMCA.Store.CI.slnf:53`; `main.bicep:260` (`sloAlertSpecs`), `:527` (workbook); `infra/OPERATIONS.md:16`; `Tests/Hosts/MMCA.Store.Gateway.Tests/GracefulShutdownTests.cs` (a sealed subclass; the assertion body lives in `MMCA.Common.Testing/GracefulShutdownTestsBase.cs`) |
| 14 | Testability & Test Strategy | 3 | 4 | 9 | 12/27 | Non-inverted pyramid + arch fitness + integration tier gating deploy; the unit-tier coverage floor is 51.6 measured on Store's own code (reportgenerator `+MMCA.Store.*;-*.Tests`, ~54% actual), the same self-filtered gate shape as ADC. A nightly real-broker round-trip tier (`MMCA.Store.CrossService.IntegrationTests`, Testcontainers RabbitMQ+SQL, `cross-service-tests.yml`) covers the outbox-to-broker-to-consumer flow the in-process tests only approximate. Held at 9 (not 10): the broker round-trip is non-gating nightly and the SQL integration suite runs only in CI. Evidence: `deploy.yml:232-258` (coverage-floor step; floor 51.6 at `:255`, self-filter at `:253`), `:220` (the `MMCA.Store.CI.slnf` test run); arch tests; `Common.Testing.E2E` |
| 15 | Best Practices & Code Quality | 2 | 4 | 8 | 8/16 | Five analyzers at error + TWAE + CPM; targeted vuln pin; consistent Result pattern; zero TODO/HACK/FIXME in `Source/`, every hand-written pragma carries an inline reason. Suppression hygiene is documented: every global `NoWarn` code carries an inline reason (`Directory.Build.props:22-36`: CS1591 `:22-26`, EXTEXP0001 `:27-31`, S8970 `:32-35`, global line `:36`), RMG020 sits in a documented `.Application`-only group (`:55-63`), the test-tier suppressions are centralized (`:45-53`) and no test csproj declares its own `NoWarn`; `NuGetAuditSuppress` is three `System.Private.Uri` advisories with a stated reason (`:8`). The weekly `maui-audit.yml` covers the MAUI head's vulnerability audit (§32 substance by its own header, `:4`). Held at 8, not 9, on criterion 1 alone: the MAUI head is in neither solution filter (`MMCA.Store.CI.slnf:24-25`, `maui-audit.yml:17` and `.editorconfig:841` all say so), so its analyzers/TWAE are review-only, an isolated gap in the 7-8 band. Evidence: `Directory.Build.props:16-20,102-123`; `Directory.Packages.props:62-65,74` (the five analyzers), `:115-116` (targeted OpenTelemetry.Api vuln pin); `.editorconfig:312`; `deploy.yml:469` (the gating vulnerability audit) |
| 16 | AI-Native Application Architecture | 2 | N/A | N/A | N/A | Rubric v2 ([ADR-110](../adr/110-rubric-v2-category-realignment.md)): N/A, no product feature calls a language model (the workspace's agentic engineering tooling is scored in §33); weight 2 leaves both denominators until a feature does. The Maintainability & Evolvability criteria this number carries under rubric v1 score under §34 (coupling, tech-debt register), §32 (lockstep upgrades) and §33 (onboarding). The lockstep invariant is executable: `FrameworkVersionConsistencyTests` asserts every `MMCA.Common.*` pin shares one version and fails the build on a partial sweep, in the CI merge gate. Evidence: `Tests/Architecture/MMCA.Store.Architecture.Tests/Governance/FrameworkVersionConsistencyTests.cs`; `MMCA.Store.CI.slnf:53`; `Directory.Packages.props:8-105` |
| 17 | DevOps & Deployment | 2 | 4 | 9 | 8/18 | CI gates (build/analyzers/tests/coverage-floor/model-drift) then an integration gate then deploy with **post-deploy smoke + auto-rollback**; two-phase Bicep; OIDC managed identity; cost-guard/dr-drill/load-test workflows. MI-SQL is active in production: the full `infra/SQL-MANAGED-IDENTITY.md` sequence is complete (repo variable `USE_MANAGED_IDENTITY_SQL=true`, activation deploy run 29192048197 green with the full gate chain + smoke), so all three services authenticate passwordless via managed identity; the SQL password path remains only as the documented dual-auth rollback. The deploy path admits no zero-test production rollout: `backend-test-gate` runs the `MMCA.Store.CI.slnf` unit + architecture + bUnit tier under the exact complement of `e2e-gate`'s condition (`deploy.yml:347`, `if` at `:349` against e2e-gate's `:645`), so precisely one of the two runs on every code deploy; it is in `deploy.needs` (`:999`) and the deploy condition tolerates `skipped` from both (`:1038-1039`); it mirrors ADC's job (ADR-064). Held at 9, not 10: no Bicep validate/what-if runs before the prod deploy, so an infra-only PR merges with the template unexecuted; SQL `publicNetworkAccess: Enabled` with the AllowAzureServices rule at `infra/main.bicep:590,611-616`, the identical caveat holding ADC at 9; single prod-only environment, `ENVIRONMENT_NAME: prod` hardcoded at `deploy.yml:28`. Evidence: `deploy.yml:1073,1170-1186` (MI-SQL wiring: the `USE_MANAGED_IDENTITY_SQL` env and the Stage 1 / Stage 3 parameter folds), `:999` (deploy gate chain), `:1227` (smoke), `:1235`/`:1333` (rollback app list and loop); `infra/SQL-MANAGED-IDENTITY.md`; repo vars `USE_MANAGED_IDENTITY_SQL`/`SQL_AAD_ADMIN_*` |
| 18 | UI Architecture & Components | 3 | 4 | 8 | 12/24 | Code-behind split, `@inherits DataGridListPageBase<ProductDTO>`, scoped cart service owns data/behavior, reuse of Common.UI primitives. Maturity 4: the container/presentational conventions are machine-enforced pre-merge by `UIArchitectureConventionTests` (sealed subclass of the shared base, 400-line code-behind cap + 120-line inline `@code` cap, non-vacuous `MinimumCodeBehindFiles` guard; largest code-behind is 368 lines), running in the deploy-gating `MMCA.Store.CI.slnf` on push and PR, the same mechanism as ADC's M4. Held at 8: minor inline-style logic in markup remains. Evidence: `Tests/Architecture/MMCA.Store.Architecture.Tests/Ui/UIArchitectureConventionTests.cs:9`; `StoreArchitectureMap.cs:27,35,43`; `MMCA.Store.CI.slnf:53`; `deploy.yml:220,375`; `ProductList.razor.cs:15`; `CartDrawer.razor:49` |
| 19 | State Management & Data Flow | 3 | 4 | 8 | 12/24 | Single source of truth, scoped (no static cross-user state), unidirectional flow with `OnChange`+`InvokeAsync(StateHasChanged)`+`Dispose`, single-flight token hydrate. Maturity 4: both §19 red flags are machine-enforced pre-merge by `StateManagementConventionTests` (mutable-static-state reflection scan over the `Layer.Ui` assemblies with a non-vacuous guard, plus the singleton-`*StateService` source scan), in the deploy-gating `MMCA.Store.CI.slnf`; `CartStateService` is registered `TryAddScoped`, proving the rule the gate enforces. Drawer visibility goes through the notify path: `ICartStateService.cs:45` is getter-only with `OpenDrawer`/`CloseDrawer`/`ToggleDrawer` at `:94-100`, both setters notify at `CartStateService.cs:332-342` behind a `private set` at `:69`, and `CartDrawer.razor:3` binds one-way. The superseded-load generation guard covers all seven detail pages (`OrderDetail.razor.cs:88`, `ProductDetail.razor.cs:80`, `CatalogProductDetail.razor.cs:86`, `ShoppingCartDetail.razor.cs:83`, `CategoryDetail.razor.cs:85`, `CustomerDetail.razor.cs:110`, `InventoryItemDetail.razor.cs:79`) plus `CatalogBrowse.razor.cs:189` and `UserDetail.razor.cs:45,97,105`. Held at 8, not 9: there is no client-side staleness or invalidation strategy; the `IUiReadCache` opt-in is withdrawn from all six UI entity services (commit `e60bb8be`) pending a per-service invalidation story (`ShoppingCartService.cs:18` constructs `EntityServiceBase` with no read cache), so the rubric's cached-with-invalidation criterion is carried by the server output cache alone; the lever for 9 is not yet identified. Evidence: `Tests/Architecture/MMCA.Store.Architecture.Tests/Ui/StateManagementConventionTests.cs:10`; `Sales.UI/DependencyInjection.cs:34` (`TryAddScoped`); `MMCA.Store.CI.slnf:53`; `CartStateService.cs:63` (the `OnChange` publisher); `CartButton.razor:67,69` (`InvokeAsync(StateHasChanged)` + `Dispose`); Common `MMCA.Common.UI.Web/Services/ServerTokenStorageService.cs:45-54` (the single-flight hydrate is framework code, not Store code) |
| 20 | Design System & UI Consistency | 2 | 4 | 8 | 8/16 | MudBlazor + Common.UI theme/tokens used consistently, shared grid-paging wrapper; the brand-color token convention is CI-enforced by `BrandColorTokenTests` (sealed subclass at `Tests/Architecture/MMCA.Store.Architecture.Tests/Ui/BrandColorTokenTests.cs:10`, both StoreHome stylesheets embedded into the test assembly at `MMCA.Store.Architecture.Tests.csproj:10`, in `MMCA.Store.CI.slnf:53`). No `Style=`/`CellStyle=` attribute remains under `Source/**/*.razor`: the values ride the shared semantic classes defined once in `Source/Hosts/UI/MMCA.Store.UI.Web/wwwroot/store.css:28` (with a comment on why no `!important` is needed) and mirrored in the MAUI head's `MMCA.Store.UI/wwwroot/app.css:38`, Common's `.detail-card`, or MudBlazor's own `d-flex`/`flex-column`/`flex-1` utilities; the list pages consume them (`CategoryList.razor:24` `.list-search-field`, `:95` `CellClass="grid-cell-actions"`; `CustomerList.razor:82`; `ReviewList.razor:110`), and a bUnit fact pins the sweep (`Tests/Modules/Catalog/MMCA.Store.Catalog.UI.Tests/Pages/Categories/CategoryListTests.cs:127` asserts the action column renders `td.grid-cell-actions`), in `MMCA.Store.CI.slnf:40`, executed by the required `build-and-test` check (`deploy.yml:244`). Maturity 4: the token guard and the bUnit pin are both merge gates. Held at 8, not 9: the StoreHome landing stylesheet hard-codes a hex palette behind `!important` with only `--mmca-primary` tokenized (`UI.Web.Client/Pages/StoreHome.razor.css:218,289`; the MAUI copy carries the same 17 six-digit literals), the rubric's library-defaults-fought red flag in one isolated place, which is the 7-8 band's "minor, isolated gap" rather than reference quality. Evidence: `App.razor:11`; `BrandColorTokenTests.cs:10`; `store.css:28`; `app.css:38`; `CategoryList.razor:24,95`; `CustomerList.razor:82`; `ReviewList.razor:110`; `CategoryListTests.cs:127`; `StoreHome.razor.css:218,289` |
| 21 | Accessibility (a11y) | 3 | 3 | 8 | 9/24 | Strong semantics/ARIA, stated WCAG 2.1 AA, **32 axe scans** (23 `ScanAsync` + 9 `ScanGridAsync`) over the public, shopper, and Catalog/Sales/Identity admin surfaces plus home in both palettes (dark-palette home scan at `AccessibilityTests.cs:327-340`), pinned to real WCAG 2.1 AA tags (`AxeOptions.cs:17-24`), and the axe suite **gates the deploy** (the chromium `e2e-gate` in `deploy.yml`'s `needs`; nightly keeps the full matrix). Maturity 3, not 4: the rubric pairs axe-in-CI with a recorded manual screen-reader pass, and the `ACCESSIBILITY-SCREENREADER-PASS.md` results log holds only the placeholder row (`:70`, header `:68-69`; the runbook's own `:75-76` names a dated row as what lifts the deduction), matching ADC. The e2e-gate is also chromium-only, UI-change-conditioned, and a SKIPPED gate is accepted by `deploy` (chromium-only at `deploy.yml:706`, UI-scoped at `:703`, skipped-tolerance at `:1241`). Held at 8, not 9: keyboard operability/focus order have zero automated coverage, every grid scan disables `aria-input-field-name` for the MudBlazor pager combobox (accepted, `AxeOptions.cs:35-46`), and dark-palette contrast is scanned on one page (`:493`). Evidence: `Tests/E2E/MMCA.Store.E2E.Tests/Workflows/AccessibilityTests.cs:30-643`; `store-ACCESSIBILITY-SCREENREADER-PASS.md:70` (Website `docs-src/guides/`; placeholder row only); `CartDrawer.razor:9,26,33,76-99`; `deploy.yml:692-706` (`e2e-gate` job, rationale `:694-702`, UI-scoping `:703`, `browsers: '["chromium"]'` `:706`), `:1200` (`e2e-gate` in deploy `needs`), `:1241` (a SKIPPED gate is accepted) |
| 22 | Responsive & Cross-Browser | 2 | 4 | 8 | 8/16 | Fluid layouts on the design-system grid with xs/sm/md/lg breakpoints (`CatalogBrowse.razor:83`), uniform grid→mobile-card reflow via `MobileInfiniteScrollList` on all seven admin list pages (`ProductList.razor:26-32` plus the Category, Review, Order, InventoryItem, ShoppingCart and Customer lists), and a defined, Playwright-verified chromium/firefox/webkit matrix (`e2e.yml:139-142`). Maturity 4: cross-engine coverage is CI-enforced by a `cross-browser-freshness` job (`deploy.yml:924`, rationale `:915-923`) that walks completed `e2e.yml` runs for the latest successful `E2E (firefox)` and `E2E (webkit)` jobs inside a 10-day window (`FRESHNESS_DAYS` at `:936`), sits in `deploy.needs` (`:1200`) and is a hard condition of the `deploy` job (`needs.cross-browser-freshness.result == 'success'`, `:1238`), the same mechanism that earns ADC its M4. Both engines held in-window green proofs at the evidence date (webkit run 35192784223, firefox run 35008122578). The per-deploy `e2e-gate` is chromium-only (`:706`, job `:692`) and the scheduled non-chromium legs are `continue-on-error` (`e2e.yml:149`; crons `:46-47`, Monday firefox `:140`, Thursday webkit `:141`), so the gate proves recency rather than that this exact tree passes on those engines, and a `workflow_dispatch` break-glass can skip it with a justification (`deploy.yml:949-961`); both are priced caveats in the backlog (a TD under #22 and the Deliberate and accepted entry), the same treatment ADC's row carries, not a cap. Held at 8, not 9, on two partial criteria: density is hard-coded `Dense="true"` across the grids with no user-selectable comfortable/compact option (`ProductList.razor:61`, `ReviewList.razor:57`), and there is no coarse-pointer/touch-target CSS and no mobile-viewport E2E coverage (zero `SetViewportSize` hits under `Tests/E2E`), so the mobile layout is implemented but not automatically verified. Evidence: `deploy.yml:924,936,949-961,1200,1238` (`cross-browser-freshness`), `:692,703,706` (`e2e-gate`); `e2e.yml:46-47,139-142,149`; `ProductList.razor:26-32,61`; `CatalogBrowse.razor:83` |
| 23 | Front-End Performance | 2 | 4 | 8 | 8/16 | Server paging/debounce/`@key`/output-cache on admin grids; the public `CatalogBrowse` uses server-side paging (`GetPagedAsync` + `MudPagination`) and cart enrichment resolves names via the targeted by-variant-id batch lookup. Maturity 4: the Core Web Vitals budgets are HARD assertions (`WebVitalsTests.cs:76-79`, `Assert.True` per metric, no soft mode) running unfiltered inside the deploy-gating chromium `e2e-gate` (`deploy.yml:999` `needs`), the evidence shape ADC credits at M4; this category's enforcement is per-deploy and independent of §12's k6 cadence. Evidence: `Tests/E2E/MMCA.Store.E2E.Tests/Workflows/WebVitalsTests.cs:76-79`; `deploy.yml:634-649,999`; `CatalogBrowse.razor.cs`; `CartStateService.cs` |
| 24 | Forms, Validation & UX Safety | 2 | 4 | 8 | 8/16 | Unsaved-changes guard with current-state accessor (9 pages), double-submit blocked, all states designed, destructive confirm, abandoned-payment recovery. Maturity 4: the four admin create forms' guard/dirty/validated-`MudForm`/`Required` markers are machine-enforced by the CI-gated `FormsConventionTests` (`MinimumCreateForms=4`), matching ADC on the same evidence; the four create forms source their field rules from a DataAnnotations form model through Common's `ModelValidation` bridge. Held at 8: client validation is DataAnnotations-level, not full FluentValidation parity with the server rules, the impl 8→9 lever. Evidence: `Tests/Architecture/MMCA.Store.Architecture.Tests/Ui/FormsConventionTests.cs`; `MMCA.Store.CI.slnf:53`; `ProductCreate.razor:8,58`; `CartStateService.cs:233` |
| 25 | Navigation & Information Arch | 2 | 4 | 8 | 8/16 | Bookmarkable typed routes, server-enforced `[Authorize]`/`AuthorizeView` (not UI-only), 404 handled, breadcrumbs, SSR session-cookie so deep-links/F5 don't bounce; role-guard regression is CI-gated by the three per-module `*RouteAuthorizationTests` (commit `c4adff2`), and per-actor navigation is documented in `NavigationFlow.md`. Maturity 4: all three maturity legs are live: the sealed per-module `*RouteAuthorizationTests` are non-vacuous (`CatalogRouteAuthorizationTests.cs:27` floor 2, `SalesRouteAuthorizationTests.cs:35` floor 2 plus the shared-`/orders` not-admin-gated guard, Identity on the shared base floor of 1), executed by the required `build-and-test` check at `deploy.yml:244` and by `backend-test-gate` at `:404`; doc drift scores on the implementation axis, not maturity. The doc matches the shell: `store-NavigationFlow.md:185` reads "only the routes shown in their diagram above, plus the framework-owned routes below", documenting `/profile/sessions` at `:189` (rendered for every signed-in user by `MMCA.Common.UI/Layout/NavMenu.razor:142`) and `/notifications/send` at `:190` (routable behind a bare `[Authorize]`, `MMCA.Common.UI/Pages/Notifications/NotificationSend.razor:6`, with no notification services wired in the Store UI host, `UI.Web/Program.cs:191`), the latter tracked as a `[C→A]` framework feature gate. Held at 8; the lever for 9 is not yet identified. Evidence: `ProductList.razor:3`; `CatalogRoutePaths`; `Tests/Modules/{Catalog,Sales,Identity}/MMCA.Store.*.UI.Tests/*RouteAuthorizationTests.cs` in `MMCA.Store.CI.slnf:40,46,52` |
| 26 | Front-End Security | 3 | 4 | 9 | 12/27 | Access token in-memory, refresh token in HttpOnly cookie (no localStorage), hardened origin-pinned CSP (no `unsafe-inline` script-src in prod, frame-ancestors none), runtime `/client-config` fetch (no secrets in bundle), `UseAuthenticatedNoStore`. Evidence (the implementation lives in Common and is consumed byte-identically here): Common `MMCA.Common.UI.Web/Services/ServerTokenStorageService.cs`; Common `MMCA.Common.UI/Services/Auth/WasmTokenStorageService.cs`; Common `MMCA.Common.UI.Web/Security/BlazorCspPolicyProvider.cs` |
| 27 | Internationalization (i18n) | 1 | 4 | 8 | 4/8 | Full ADR-027 adoption (supersedes ADR-011): en-US + es `.resx` pairs across all three module UIs, the API error resources, both StoreHome hosts, and the three UI modules' nav items (30 base / 30 `.es` siblings), `UseCommonRequestLocalization` (`UI.Web/Program.cs:148`, registering both supported cultures and UI cultures, `MMCA.Common .../WebApplicationExtensions.cs:141`) + `MapCultureEndpoint` (`:181`), per-module `AddErrorResources` (`Identity.Service/Program.cs:198`), `User.PreferredCulture` + `AddUserPreferences` migration. No hard-coded literal ships: snackbars use whole-sentence page keys (including the cart/checkout/Stripe strings; raw `{ex.Message}` never surfaces), breadcrumb labels localize from `Breadcrumb.*` keys built in `OnInitialized`, nav menus localize via `NavItem.TitleResource`, and MudBlazor built-in chrome localizes via the framework's `ResxMudLocalizer`. Money formatting is culture-aware: `MoneyExtensions.cs` resolves the symbol from the price's own currency (`MMCA.Common .../MMCA.Common.UI/Extensions/MoneyExtensions.cs:18-20,54-59`) and formats under `culture ?? CultureInfo.CurrentCulture` (`:54`, `:90`), inside the v1.206.0 pin (`Directory.Packages.props:23`); dates and ratings also format under `CurrentCulture`. Maturity 4, doubly gated: both arch gates run un-skipped with live thresholds (`TranslationCompletenessTests.cs:17` `MinimumBaseResources=25`, `LocalizedTextConventionTests.cs:18` `MinimumScannedFiles=40`) in the required `build-and-test` check via `MMCA.Store.CI.slnf:53` + `deploy.yml:244`, and the pseudo-loc E2E suite rides the deploy-gating `e2e-gate` (job `deploy.yml:692`, chromium-only `:706`, in deploy `needs` at `:1200`; `e2e.yml:389` runs the project unfiltered): `PseudoLocalizationTests` activates `qps-Ploc` via the production `/culture/set` cookie mechanism and asserts the `[!!` sentinel, the no-horizontal-overflow expression, and a per-page en-US leak probe over `/`, `/catalog`, and `/login` (`Tests/E2E/MMCA.Store.E2E.Tests/Workflows/PseudoLocalizationTests.cs:57`). Held at 8, not 9: pluralization is only partially mechanism-driven. Common ships `StringLocalizerPluralExtensions.cs:46` (`.One`/`.Other` suffix resolution) and the cart strings have plural keys, but `ProductReviewsPanel.razor:10` renders "(1 reviews)" from a single-form key (`ProductReviewsPanel.resx:9`), `CatalogBrowse.resx:31` is a second unpluralized count key (consumed at `CatalogBrowse.CardFormatting.razor.cs:45`), the `"(s)"` keys survive (`CartDrawer.resx:22` `"{0} item(s)"`, `ShoppingCartList.es.resx:11` `"{0} articulo(s)"`), and the Spanish plural values drop the accent (`ShoppingCartList.es.resx:12` against the accented base key at `:11`; `CartDrawer.es.resx:23` against `:9`), a user-visible defect recorded as a TD under #27. Layout tolerance is proven on three public pages (`PseudoLocalizationTests.cs:41`), with no authenticated surface and no RTL locale. Evidence: `Tests/Architecture/MMCA.Store.Architecture.Tests/Ui/{TranslationCompletenessTests.cs,LocalizedTextConventionTests.cs}`; `Sales.UI/.../CartDrawer.razor.cs` + `OrderDetail.razor.cs` (whole-sentence `Snackbar.*` keys); `CatalogUIModule.cs` (`TitleResource` + resx pair); `CatalogBrowse.razor.cs:302` (price display) |
| 28 | Front-End Testing & Quality | 3 | 4 | 8 | 12/24 | bUnit `.UI.Tests` gate the deploy (CI.slnf), broad state-coverage component tests (loading/empty/error/edge), page-level render/parameter/event tests, shared `Common.Testing.E2E`, and the Playwright suite **gates the deploy** (chromium `e2e-gate`; firefox/webkit stay advisory on the nightly matrix). bUnit breadth is 364 facts across 47 files. Held at 8: no markup or visual-regression tier exists. Evidence: `Tests/Modules/{Catalog,Sales,Identity}/MMCA.Store.*.UI.Tests`; `deploy.yml` (`e2e-gate` in deploy `needs`); `e2e.yml` (`workflow_call` + browsers input) |
| 29 | Resilience & Business Continuity | 3 | 4 | 9 | 12/27 | RTO/RPO per scenario, PITR(7d geo-redundant)+LTR, a **drilled** restore (recorded PASS 28.9 min), SLO alerts, post-deploy smoke then rollback, single-region risk explicitly accepted. Maturity 4: a `dr-freshness` job is in `deploy.needs` and fails the deploy when the last successful `dr-drill` is stale, backed by a weekly `dr-drill.yml` cron; the CI-gated `GracefulShutdownTests` in `MMCA.Store.Gateway.Tests` covers graceful shutdown. Held at 9, not 10: the `DISASTER-RECOVERY.md` drill-history table (`:186-189`) lags the weekly `dr-drill.yml` runs that satisfy `dr-freshness`, a documentation-currency gap. Evidence: `.github/workflows/deploy.yml:999` (`dr-freshness` in `deploy.needs`), `:1033` (its `success` condition); `.github/workflows/dr-drill.yml:33` (weekly cron); `Tests/Hosts/MMCA.Store.Gateway.Tests/GracefulShutdownTests.cs`; `infra/DISASTER-RECOVERY.md:186-189` (drill-history table, the 28.9-min PASS row at `:186`) |
| 30 | Compliance, Privacy & Governance | 2 | 4 | 8 | 8/16 | Real erasure path (Delete+Anonymize in one UoW, ADR-005), data export, residency + PII enforced by fitness functions, auto-stamped audit fields. Held at 8: consent and lawful basis have no implementation and the data-subject-rights surface is API-only. Evidence: `DeleteUserHandler.cs:41-67` (`OnAfterSoftDeleteAsync`); `Customer.cs:20`/`User.cs:17` (`IAnonymizable`); `Governance/DataResidencyTests.cs`; `PRIVACY.md:57-85` |
| 31 | Cost Efficiency / FinOps | 2 | 4 | 8 | 8/16 | Right-sized Basic per-service DBs, automated surge-drift guard, IaC budget + alerts, telemetry span filtering, resource tags. Maturity 4: `cost-guard` is a `workflow_call` job in `deploy.needs` (`deploy.yml:678`, `:1200`) that must be exactly `success` (`:1234`), plus the Monday cron (`cost-guard.yml:21`). The IaC cost posture: all five container apps at 0.25 vCPU / 0.5 Gi (`infra/main.bicep:1006,1162,1287,1412,1511`), 25% head-based trace sampling (`:184`), metric export at 300s (`:221-222`), the legacy `MMCAStore` database archived to a bacpac and absent from IaC (`:620-624`), Log Analytics at 30-day retention with a 1 GB/day cap (`foundation.bicep:36-42`), and a daily ACR purge task with a buildcache-only second step (`:88-98`). Held at 8, not 9: the surge guard is read-only (`cost-guard.yml:7` "never mutates anything"; `:94` prints a manual reset), so the rubric's automated-or-scheduled revert for reversible scale events is unmet in its exact terms (the identical basis holding ADC's §31 at I8), and spend attribution per service is coarse (one uniform `commonTags` set, `costCenter: 'store'`, for every resource at `:113-119`; a whole-RG budget with no tag filter at `:548-576`). Evidence: `cost-guard.yml:7,21,94`; `deploy.yml:678,1200,1234`; `infra/main.bicep:87-99` (alert email + `monthlyBudgetAmount` params), `:113-119` (tags), `:548-576` (budget, 80% actual / 100% forecast), `:1006`; `infra/foundation.bicep:36-42,88-98` |
| 32 | Dependency & Supply-Chain | 2 | 4 | 9 | 8/18 | Vulnerability auditing **is** CI-gated twice: NuGetAudit raises advisory warnings at restore and `TreatWarningsAsErrors` promotes them to errors (failing `build-and-test` and every Docker image build), and the `supply-chain` job's suppress-aware audit **gates** (exit 1 on any non-suppressed advisory); accepted advisories are the documented `NuGetAuditSuppress` set. Strong provenance: 55 committed `packages.lock.json` + CPM + lockstep Common + transitive MassTransit-v8 pin + `auditSources` config, and all three CI restores run `--locked-mode` (`deploy.yml:42,123,222`) so committed-lock drift fails the build. The SBOM is a hard gate (`43d733f`); the license report is report-only by design. The MAUI graph is covered by `maui-audit.yml`: a weekly (Sundays 06:00 UTC, `:35`) suppress-aware `dotnet list package --vulnerable` over `MMCA.Store.UI` on `net10.0-android` that exits 1 on any non-suppressed advisory (`maui-audit.yml:103-151`), the one graph the gating `supply-chain` job cannot see because the MAUI head is excluded from `MMCA.Store.CI.slnf`; it is a scheduled audit, not a deploy gate, the Apple TFMs are uncovered on a Linux runner (`:19-25`), and it mirrors ADC's `maui-audit.yml` (TD-18). No §32 mechanism behind ADC's I9 is missing here (identical gating audit/SBOM, byte-identical suppress/lock config); held below 10 by the same transitive-only MassTransit pin caveat as ADC. Evidence: `deploy.yml:42,123,222,133-162`; `Directory.Build.props:8-12,27,39`; `nuget.config` |
| 33 | Developer Experience & Inner Loop | 2 | 4 | 8 | 8/16 | Aspire one-command local stack (SQL/Redis/RabbitMQ/MailDev/Stripe), cross-repo `local.props`, fast CI slnf, editorconfig parity, local→Azure parity. Evidence: `CLAUDE.md:13-90`; `local.props.template`; `MMCA.Store.CI.slnf` |
| 34 | Architecture Governance & Docs | 2 | 4 | 9 | 8/18 | Exemplary *automated* conformance (44 arch fitness-test classes over shared bases + Store-local guards, the same inventory as ADC's) + a thorough current CLAUDE.md + ADRs canonical in `../adr/`, count/range owned by `../adr/README.md` (`MMCA.Common/FACTS.md:43-44` disclaims owning the ADR range), completed by the two-axis scorecard + `RemediationBacklog.md` (the single per-repo ledger), both committed and kept current per release. Evidence: `Tests/Architecture/MMCA.Store.Architecture.Tests/` (46 non-`obj` `.cs` files: 44 test classes + `StoreArchitectureMap.cs` + `GlobalUsings.cs`); `CLAUDE.md`; committed `ArchitectureScorecard.md` + `RemediationBacklog.md`; ADRs in `../adr/` (indexed by `../adr/README.md`) |

> **Weighted** = Maturity·weight / Implementation·weight.

## Scoring notes

- **Two axes per category:** Maturity (0-4, process/governance) and Implementation (0-10, substance/execution). The indices are computed deterministically from the table: every non-N/A category is in both denominators.
- **N/A (excluded from denominators):** §16 AI-Native Application Architecture (rubric v2, [ADR-110](../adr/110-rubric-v2-category-realignment.md): no product feature calls a model); its weight 2 leaves both denominators, Σweight 79.
- **§10** is Messaging & Integration Architecture under rubric v2, at the v2 default weight 3.
- **§27** is scored: ADR-027 (en-US + es with a CI-gated translation-completeness fitness function) supersedes ADR-011's single-locale exclusion.
- **§32 weight = 2** (default; 3 only for the published framework MMCA.Common).
- The implementation index reads directly against 100%: a 10 is awardable for an almost perfect implementation. The denominators stay ×4 and ×10 so the trend line stays comparable across re-scores.
- **Axis calibration:** a new CI gate is an enforcement gain and scores on the maturity axis, not the implementation axis; a CI-cadence change is not a substance regression; documentation drift scores on the implementation axis.
- **Axis-gap reading:** §21 and §12 are M3/I8, strong substance with enforcement short of automatic, which is the healthy direction of the gap. The maturity-over-implementation gap sits in the 18 categories at M4/I8, where enforcement is automatic but substance is short of 9.

## Cross-repo comparison

How Store relates to MMCA.Common (the framework) and MMCA.ADC (the sibling consumer) is maintained once, for all three repos, in the workspace-internal `Docs/Architecture/CrossRepoComparison.md` (not published).
