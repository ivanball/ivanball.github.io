# MMCA.ADC: Architecture Remediation Backlog

Derived from [`ArchitectureScorecard.md`](../governance/adc-ArchitectureScorecard.md) (evidence as of 2026-09-22 at MMCA.Common.* v1.209.0): **Maturity 97.5% (316/324) / Implementation 86.0% (697/810)**, weight total 81, no N/A category. Work is ranked on both scorecard axes, one band per axis. **Maturity band:** every category scoring maturity < 4, ranked by `(4 - maturity) x weight`. **Implementation band:** every category scoring implementation <= 8, ranked by `max(0, 9 - implementation) x weight` (target 9: a 10 is awardable at re-score time for an almost perfect implementation, but ranking against 10 would drown the real gaps, so 9 governs scheduling). Ties break by weight descending, then category number. Today 3 categories sit in the maturity band (8 weighted points), 14 in the implementation band (34 gap points), 31 score maturity 4 (protect, do not regress) and 19 of those also score implementation >= 9, which is the only combination that is done on both axes. This is the single remediation and tech-debt ledger: each deferred sub-item keeps its `TD-NN` ID with its blocker, resolution path and effort. **Effort key:** **S** = hours · **M** = about 1 day · **L** = multi-day. Right-size every fix to the measured conference load (~76 accounts / ~67 peak concurrent).

## Open work at a glance

| Priority | # | Category | M / I | Band | Next action |
|---|---|---|---|---|---|
| 4 | §22 | Responsive & Cross-Browser | 4 / 7 | Implementation | Adopt the DataGrid density toggle and give the 19 raw-table pages a phone-breakpoint degradation |
| 3 | §7 | Microservices Readiness | 4 / 8 | Implementation | Lever not yet identified; name it at the next re-score |
| 3 | §10 | Messaging & Integration Architecture | 3 / 8 | Maturity and implementation | Alert, runbook and replay procedure for the broker error queue |
| 3 | §11 | Security | 4 / 8 | Implementation | Lever not yet identified (the threat model is deliberately not scheduled) |
| 3 | §18 | UI Architecture & Components | 4 / 8 | Implementation | TD-16: systematic presentational sub-component extraction |
| 3 | §21 | Accessibility (a11y) | 3 / 8 | Maturity and implementation | Record the dated manual screen-reader pass |
| 2 | §5 | Vertical Slice Architecture | 4 / 8 | Implementation | Lever not yet identified; name it at the next re-score |
| 2 | §9 | API & Contract Design | 4 / 8 | Implementation | TD-24: OpenAPI baseline diff plus an AsyncAPI-style event document |
| 2 | §12 | Performance & Scalability | 3 / 9 | Maturity | Run the capacity proof inside the deploy chain |
| 2 | §15 | Best Practices & Code Quality | 4 / 8 | Implementation | TD-18: bring the MAUI head inside a CI build |
| 2 | §23 | Front-End Performance | 4 / 8 | Implementation | Code-split the WASM bundle and optimize images |
| 2 | §24 | Forms, Validation & UX Safety | 4 / 8 | Implementation | Client end-after-start check (BR-122) on the Session forms |
| 2 | §25 | Navigation & Information Arch | 4 / 8 | Implementation | Document `/confirm-email` and `/users/{Id:int}`; add an ADC navigation-contract test |
| 2 | §31 | Cost Efficiency / FinOps | 4 / 8 | Implementation | Automate the conference-day surge and its revert |
| 1 | §27 | Internationalization (i18n) | 4 / 8 | Implementation | Deferred by decision (see Deliberate and accepted) |
| - | §28 | Front-End Testing & Quality | 4 / 9 | Outside both bands | TD-20 browser half; pixel-level visual regression |
| - | §26 | Front-End Security | 4 / 9 | Outside both bands | TD-08 same-origin data-call proxy |

## Open items

### [ ] #10 · Messaging & Integration Architecture · M3 / I8 (weight 3; maturity priority 3, implPriority 3)
Most criteria are CI-enforced (frozen contract snapshot, gateway route-map tests in `MMCA.ADC.CI.slnf:60`, the Service Bus emulator tier gating deploys through `cross-service-freshness`, `cross-service-tests.yml:129`), but three are not: nothing requires `[EventName]` on every event, event ordering is unguarded, and broker faults are not wired to alert-as-code. Those three are the maturity 3→4 levers. M4 is also defensible; M3 is the adjudicated score.
- [ ] **Dead-letter procedure (implementation 8→9).** Wire an alert and a runbook for the broker error queue with a replay or discard procedure. `FaultIntegrationEventConsumer.cs:57` (Common) logs "faulted ... moved to the error queue", but the only messaging log alert matches "dead-lettered", the outbox text (`infra/main.bicep:521`); ADR-087 records no alert, runbook or replay path (`../adr/087-broker-poison-message-handling.md:200`) and `infra/OPERATIONS.md:189` replays only an outbox row reset. Rubric criterion: `ArchitectureEvaluationCriteria.md:336`. Effort **S-M**.
- [ ] **Ordering guard on the speaker-link handlers.** `SpeakerLinkedToUserHandler.cs:48` overwrites `LinkedSpeakerId` with no stale or ordering guard, and `SpeakerUnlinkedFromUserHandler.cs:56` guards one direction only, under a second-level redelivery reaching 3600 s on Azure Service Bus (Common `MessageBusSettings.cs:211`). Effort **S**.
- [ ] **Type-argument-aware contract snapshot.** The snapshot renders nullables as `Nullable`1` with no type argument, so a nullable retype passes (`Tests/Architecture/MMCA.ADC.Architecture.Tests/Contracts/IntegrationEventContractTests.cs:17`); the gate is keyed by CLR type name, not the `[EventName]` wire identity (Common `IntegrationEventContractTestsBase.cs:134`). The fix lands in MMCA.Common's base. Effort **S**.
- [ ] **Doc drift:** `MMCA.ADC/AGENTS.md:76` calls the emulator tier continue-on-error, contradicting the workflow. Effort **S**.

### [ ] #21 · Accessibility · M3 / I8 (weight 3; maturity priority 3, implPriority 3)
The axe layer is an enforced deploy gate (45 scans, `AccessibilityTests.cs:23`, inside the chromium `e2e-gate`), with a stated WCAG 2.1 AA target. Maturity stops at 3 because the rubric pairs automated CI checks with a recorded manual screen-reader pass. The implementation lever is not yet identified (axe is E2E-only, chromium-only, non-PR and conditional).
- [ ] **Record a dated manual screen-reader pass** in `adc-ACCESSIBILITY-SCREENREADER-PASS.md` (Website `docs-src/guides/`), whose results table holds only the empty placeholder row (`:64`). Needs a human with NVDA or VoiceOver against the running Aspire app; it cannot be done headless. This is the cheapest maturity 3→4 win.

### [~] #12 · Performance & Scalability · M3 / I9 (weight 2; maturity priority 2)
The implementation half is at target: a green k6 proof on the deployed 0.25 vCPU topology (run 35013811738; thresholds `conference-read-load.js:56-57`; topology `infra/main.bicep:1566`), verified two-replica hub fan-out (`TwoReplicaHubFanOutTests.cs:49`), and backpressure configured on all four services (`Conference.Service/appsettings.json:50`, applied by `MMCA.Common.Infrastructure/DependencyInjection.cs:1296`).
- [ ] **In-band capacity proof (maturity 3→4).** The k6 tier executes on a monthly cron plus dispatch (`load-test.yml:23`) and `load-freshness` gates on recency, not execution (`deploy.yml:904`, 35-day window `:876`, break-glass `:887-900`, in `deploy.needs` `:1305`). Run the k6 tier, or a scaled-down smoke of it, inside the deploy chain.

### [ ] #22 · Responsive & Cross-Browser · M4 / I7 (weight 2; implPriority 4, the top of the implementation band)
Maturity is gated by `cross-browser-freshness` (`deploy.yml:1034`, `:1305`, `:1347`); that gate is the maturity lever and does not count toward implementation.
- [ ] **Density options.** The framework ships the capability (`DataGridListPageBase.cs:78` `DenseGrid`, `:898` `ToggleDensity()`) and ADC adopts none of it; `EventList.razor:73` hardcodes `Dense="true"`.
- [ ] **Content reflow on the 19 non-DataGrid table pages**, starting with the data-dense conference-day surfaces (`OrganizerPointsOverview.razor:61,:98`, `OrganizerAttendance.razor:50`); the DataGrid pages already degrade to card lists. Effort **M** for both items together.

### [ ] #18 · UI Architecture & Components · M4 / I8 (weight 3; implPriority 3)
- [ ] **TD-16 (the §18 impl 8→9 lever, effort S).** The enforced cap is `MaxCodeBehindLines => 400` (`MMCA.Common/Source/Hosting/MMCA.Common.Testing.Architecture/Bases/UIArchitectureConventionTestsBase.cs:22`, unoverridden in `UIArchitectureConventionTests.cs:10-12`). The high-water code-behind is `Pages/Public/PublicSessionDetail.razor.cs` at **386** (14 lines of headroom), then SpeakerDetail 346, PublicSessionList 336, SessionLive 333, SessionFeedback 325, SessionLiveModerationPanel 316, SpeakerDashboard 314, ADCHome 313, SessionDetail 309, so one file sits within 38 lines of the cap (figures carried from the last measurement; re-measure at the next re-score). §18 holds at 8 because extraction is applied file by file rather than systematically, and the component-lifetime (render hooks) and render-fragment/slot criteria are thinly met. **Blocker:** none. **Resolution path:** presentational sub-component extraction per the TD-13 pattern, markup moved verbatim so the rendered DOM and the E2E selectors are unchanged.

### [ ] #9 · API & Contract Design · M4 / I8 (weight 2; implPriority 2)
- [ ] **TD-24 (effort M) · the API contract guard cannot fail on a breaking change, and the async contract has no published document.** `OpenApiContractTestsBase.cs:15` runs its assertions against the live document with no committed snapshot; the checks are well-formedness, a paths-count floor (`:64`) and case-insensitive presence of pinned resource strings (`:78`), with ADC pinning `MinimumPathCount=10` and `/Events`, `/Sessions`, `/Speakers` (`Tests/Integration/MMCA.ADC.Conference.IntegrationTests/Contract/OpenApiContractTests.cs:17`). A renamed property, a changed status code or the removal of a non-pinned route group all pass. Four per-service OpenAPI documents are committed (`Source/Services/*/openapi/*.json`) behind an "OpenAPI documents are current" step (`deploy.yml:297-312`) that fails on an uncommitted diff (`:309-310`) or a document count other than 4 (`:305-307`): a drift gate, not a breaking-change classifier. The seven integration events are frozen byte-exactly in the arch tier (`Tests/Architecture/MMCA.ADC.Architecture.Tests/Contracts/IntegrationEventContractTests.cs:9`) but no AsyncAPI document exists. **Blocker:** the guard base lives in MMCA.Common (`Source/Hosting/MMCA.Common.Testing/Conformance/OpenApiContractTestsBase.cs`), so a baseline-diff mode is a framework change first. **Resolution path:** a committed per-service baseline with a normalized diff assertion in the Common base (opt-in, breaking-change classes only), adopted in the four ADC integration projects, plus an AsyncAPI document generated from the frozen event list next to `/openapi/v1.json`. **Effort:** M (S in ADC once the Common half ships).

### [ ] #15 · Best Practices & Code Quality · M4 / I8 (weight 2; implPriority 2)
- [ ] **TD-18 (effort L) · the MAUI app is outside every CI build.** `MMCA.ADC.CI.slnf` lists only `UI.Web` (`:25`) and `UI.Web.Client` (`:26`); no workflow installs the `maui-android` workload, so `MMCA.ADC.UI` is never compiled in CI and its analyzers, `TreatWarningsAsErrors` and its own `NoWarn CA5392` (`Source/Hosts/UI/MMCA.ADC.UI/MMCA.ADC.UI.csproj:151`) are review-enforced only. The gating vulnerable-package audit is the shared MMCA.Common composite action at `deploy.yml:581-592` (`uses` at `:589`), scoped to `MMCA.ADC.CI.slnf` at `:591`. `.github/workflows/maui-audit.yml` audits the MAUI graph weekly (android-only, in no `deploy.needs`), so the detector half exists and the gating half does not. **Blocker:** a MAUI leg installs the `maui-android` workload on every run, against the deliberate Actions-minute budget. **Resolution path:** a scheduled (not per-PR) MAUI build leg, or wiring the audit result somewhere that can fail, verified by a full-solution package-mode restore rather than `CI.slnf`; Store carries the identical gap and should be swept in the same pass. Maturity 4 on §15 is `CI.slnf`-wide, not repo-wide, while this is open.
- Minor residual: the four-code NoWarn list `CA2234;CA1054;S3459;S1144` is duplicated across five test csproj files against the centralization intent at `Directory.Build.props:40-48`.

### [ ] #23 · Front-End Performance · M4 / I8 (weight 2; implPriority 2)
- [ ] **Code-split the WASM bundle; optimize images.** Both are open; the CWV budgets themselves are enforced (`WebVitalsTests.cs:39`).

### [ ] #24 · Forms, Validation & UX Safety · M4 / I8 (weight 2; implPriority 2)
- [ ] **Mirror the cross-field rule on the Session forms (effort S).** The server enforces BR-122 (EndsAt strictly after StartsAt, `Session.cs:207`) while `SessionCreate.razor.cs:141` submits after `ValidateAsync()` alone; the Event (`EventCreate.razor.cs:75`) and Activity (`ActivityCreate.razor.cs:167`) forms mirror their range rules. The server-rejection branch also discards the `Result` error for a generic `SaveFailed` snackbar (`SessionCreate.razor.cs:154`). Add the end-after-start client check to the Session create and edit forms. The format half of client/server parity is shared already (`SessionizeCodeAttribute.cs:41`, `EventFormModel.cs:44`).

### [ ] #25 · Navigation & Information Architecture · M4 / I8 (weight 2; implPriority 2)
- [ ] **Document `/confirm-email` and `/users/{Id:int}` (effort S).** `/confirm-email` (`Identity.UI/Pages/Users/ConfirmEmail/ConfirmEmail.razor:1`) has zero mentions in `adc-NavigationFlow.md` (655 lines), and `/users/{Id:int}` (`UserDetail.razor:1`) is covered only by the generic CRUD paragraph.
- [ ] **ADC-side navigation-contract test (optional, effort M)** modeled on Common's `NavigationContractTests`, so a new route cannot land undocumented (zero NavigationFlow/NavigationContract references in ADC `.cs`).

### [ ] #31 · Cost Efficiency / FinOps · M4 / I8 (weight 2; implPriority 2)
- [ ] **Automate the conference-day surge and its revert (effort M)**, the rubric's "reversible scale events" criterion. `cost-guard.yml:7` is detection-only and `:83` prescribes a manual reset, so no scheduled or automated surge-and-revert exists. Distinct from TD-15, the topology-collapse option under Deliberate and accepted.

### Rows with no recorded lever
§5 (the accepted layered-by-project hybrid is the cap and is not itself a lever), §7 (eight typed gRPC client registrations with two bidirectional pairs and a three-node call loop; the modernization-patterns criterion has zero occurrences) and §11 (the committed threat model is deliberately not scheduled), plus §21's implementation half. Name a lever at the next re-score; never invent one here.

### Outside both bands (the 9→10 rung, not ranked)

### [~] #26 · Front-End Security · M4 / I9
- [~] **TD-08 (effort L) · same-origin data-call proxy.** The auth-path BFF is live: the refresh token lives only in the HttpOnly cookie and is exchanged server-side (`/auth/session/token`, `UseCookieSessionRefresh`, `ICookieSessionRefresher` in MMCA.Common.API), and the access token is held in memory, hydrated through `SameOriginProxyTokenRefresher`. Open: a full same-origin data-call proxy (the access token also out of JS) and proxying the login/register/OAuth flows, which closes the window in which the refresh token transits JS during the login round-trip. **Blocker:** needs interactive verification on the Aspire stack, then a release. See `TokenStorageDesignNote.md`.

### [ ] #28 · Front-End Testing & Quality · M4 / I9
- [ ] **TD-20 (effort S-M) · the browser half of the deploy gate is conditional.** `e2e-gate` (`deploy.yml:793`) runs only when the diff is UI-affecting (`if` at `:804`) and the `deploy` job accepts a skipped gate (`:1355`; `deploy.needs` at `:1310`), so a backend-only, infra-only or script-only merge reaches production with no chromium E2E, axe scan or Core Web Vitals assertion. The zero-test-execution half is closed by `backend-test-gate` (`deploy.yml:394-396`), whose condition is the exact complement. **Blocker (deliberate):** Actions-minute budget; the post-deploy smoke gate is the named backstop. **Resolution path:** a cheap UI-independent browser smoke leg that always runs, or record the conditionality as permanent.
- [~] **Pixel-level visual regression.** The markup-snapshot half exists (`ComponentsSnapshotTests.cs:27`, 12 `[Fact]`s over 12 committed `.html` baselines, missing baseline = non-match, in `MMCA.ADC.CI.slnf:43,:49,:55`); pixel-level Playwright visual regression is open, and 12 components is a small slice of the surface.

## Implementation band

Categories scoring implementation <= 8, ranked by implPriority (target 9). §10 and §21 also hold a maturity-band row; their items are under Open items.

| implPriority | # | Category | w | Impl | Recorded lever |
|---|---|---|---|---|---|
| 4 | §22 | Responsive & Cross-Browser | 2 | 7 | Adopt the density-options criterion (zero adoption; `DataGridListPageBase.cs:78` `DenseGrid`, `:898` `ToggleDensity()`; `EventList.razor:73` hardcodes `Dense="true"`) and complete content reflow on the 19 non-DataGrid table pages (`OrganizerPointsOverview.razor:61,:98`, `OrganizerAttendance.razor:50`). Effort M |
| 3 | §7 | Microservices Readiness | 3 | 8 | Not yet identified: eight typed gRPC client registrations (`Conference.Service/Program.cs:392`, `Engagement.Service/Program.cs:281`, `Identity.Service/Program.cs:316`, `Notification.Service/Program.cs:225`), two bidirectional pairs and a three-node call loop; the rubric v2 modernization-patterns criterion (anti-corruption layer, strangler route) has zero occurrences |
| 3 | §10 | Messaging & Integration Architecture | 3 | 8 | Alert, runbook and replay procedure for the broker error queue (`FaultIntegrationEventConsumer.cs:57` vs `infra/main.bicep:521`); ordering guard on `SpeakerLinkedToUserHandler.cs:48`; a contract snapshot that keeps nullable type arguments (`IntegrationEventContractTests.cs:17`) |
| 3 | §11 | Security | 3 | 8 | Not yet identified: no threat model (a committed model is deliberately not scheduled), service-to-service identity on five of seven gRPC surfaces (`BookmarkCountsGrpcService`, `Engagement.Service/Program.cs:341`, and `LiveChannelGrpcService`, `Notification.Service/Program.cs:290`, are `AllowAnonymous` by network position), an FCM client key committed at `MMCA.ADC.UI/appsettings.json:34` (deliberate, minor). The accepted SQL public-network exposure (`infra/main.bicep:764`, `:770`, `:779`) is the 9→10 rung |
| 3 | §18 | UI Architecture & Components | 3 | 8 | **TD-16**: the high-water code-behind is `PublicSessionDetail.razor.cs` at 386 of the 400 cap; systematic presentational sub-component extraction. Effort S |
| 3 | §21 | Accessibility (a11y) | 3 | 8 | Not yet identified: axe is E2E-only, chromium-only, non-PR and conditional (45 scans, `AccessibilityTests.cs:23`); the screen-reader pass is the maturity half |
| 2 | §5 | Vertical Slice Architecture | 2 | 8 | Not yet identified: the accepted layered-by-project hybrid (DTOs in Shared with horizontal mapper/validator folders) is the recorded cap and is not itself a lever |
| 2 | §9 | API & Contract Design | 2 | 8 | **TD-24**: an OpenAPI baseline diff (`OpenApiContractTestsBase.cs:15`, `:64`, `:78`; ADC subclass `OpenApiContractTests.cs:17`) plus an AsyncAPI-style document for the seven events frozen in `IntegrationEventContractTests.cs:9`. Effort M |
| 2 | §15 | Best Practices & Code Quality | 2 | 8 | **TD-18**: the MAUI head is in no CI build (`MMCA.ADC.CI.slnf:25`); `maui-audit.yml` runs weekly, android-only, in no `deploy.needs`, so the detector exists and the gate does not. Minor: the NoWarn list duplicated across five test csproj files (`Directory.Build.props:40-48`) |
| 2 | §23 | Front-End Performance | 2 | 8 | Code-split the WASM bundle; optimize images |
| 2 | §24 | Forms, Validation & UX Safety | 2 | 8 | Client end-after-start check (BR-122, `Session.cs:207`) on the Session create and edit forms (`SessionCreate.razor.cs:141`), and surface the server `Result` error instead of a generic `SaveFailed` snackbar (`:154`). Effort S |
| 2 | §25 | Navigation & Information Arch | 2 | 8 | Document `/confirm-email` (`ConfirmEmail.razor:1`) and `/users/{Id:int}` (`UserDetail.razor:1`) in `adc-NavigationFlow.md` (effort S); an ADC-side navigation-contract test modeled on Common's `NavigationContractTests` (optional, effort M) |
| 2 | §31 | Cost Efficiency / FinOps | 2 | 8 | Automate the conference-day surge and its revert into a scheduled scale event (`cost-guard.yml:7` is detection-only, `:83` a manual reset). Effort M |
| 1 | §27 | Internationalization (i18n) | 1 | 8 | Deferred by decision: pseudo-localization covers 3 public pages (`PseudoLocalizationTests.cs:51-56`, public by design `:31`); do not re-propose without new evidence (re-open triggers under Deliberate and accepted) |

**Σ implPriority = 34** across the 14 live rows above (4+3+3+3+3+3+2+2+2+2+2+2+2+1); ties are broken by weight descending, then category number ascending. The index gap to a full 810 is 113 weighted points; the 34 are the scheduling gap to 9 on every category.

## Maturity band

| Priority | # | Category | w | Maturity | Recorded lever |
|---|---|---|---|---|---|
| 3 | §10 | Messaging & Integration Architecture | 3 | 3 | Require `[EventName]` on every event, guard event ordering, wire broker faults to alert-as-code |
| 3 | §21 | Accessibility (a11y) | 3 | 3 | Record the dated manual screen-reader pass (`adc-ACCESSIBILITY-SCREENREADER-PASS.md:64`) |
| 2 | §12 | Performance & Scalability | 2 | 3 | An in-band capacity proof: the k6 tier inside the deploy chain rather than behind the recency-only `load-freshness` gate (`load-test.yml:23`, `deploy.yml:904`) |

Σ maturity priority = 8 across the 3 rows (3+3+2).

## Deliberate and accepted

Recorded decisions, not scheduled work.

- **Single-region deployment** (no multi-region failover): accepted in `infra/DISASTER-RECOVERY.md`, with the single SQL server and single Container Apps environment as accepted single points of failure; the measured load does not justify the cost.
- [ ] **No conference-day `minReplicas:2` for the gateway/UI:** the measured load does not warrant it; recorded as accepted risk in `infra/DISASTER-RECOVERY.md`. `cost-guard.yml` flags a surge that is applied and not reverted.
- **No interactive OpenAPI UI (Scalar/Swagger):** the h2c-only REST services are not browser-reachable directly; a Gateway-routed UI is a small follow-up if wanted.
- **Legacy pre-cutover database retained**, untouched at Basic tier, as the rollback and archive source; never written to after the per-service cutover.
- **Integration events published post-commit carrying DB-generated IDs:** intentional, since the event must carry the persisted identity.
- **Constructor-dependency ceilings (§1):** `AuthenticationService` is a cohesive auth facade and is accepted as-is. The service ceiling is 10 (set for `AuthenticationService`, alongside `HandlerConventionTests`), the controller ceiling 10 (`Tests/Architecture/MMCA.ADC.Architecture.Tests/Cqrs/ConstructorDependencyCountTests.cs:52`) and the handler ceiling 7 (`:61`), the last set at the widest handler (`ResetPasswordHandler`, `SubmitQuestionHandler`). `SessionsController` sits at 10 on a dispensation documented in the test's XML doc (`ConstructorDependencyCountTests.cs:39-50`): its tenth dependency, `IEntityQueryService<Event, ...>`, exists only for the BR-86 advisory `X-Warning` header on create, which the framework's fixed create-handler signature cannot set. Lower it the moment a framework change makes 9 true.
- **Vertical slice, deliberate layered-by-project hybrid (§5):** cross-cutting concerns run in the decorator pipeline, slice cohesion is held by the CI-gated `SliceCohesionTests`, and the hybrid is the accepted choice that caps implementation at 8. It does not absorb enforcement-coverage gaps, which stay schedulable.
- [~] **Design-system residuals (§20), Common-side and watch item:** eight `!important` overrides live in MMCA.Common's shared `app.css` (`:189`, `:209-210`, `:297`, `:302`, `:307-308`, `:314`, each with a mobile or a11y rationale; a generalized selector at `:206`); these are MMCA.Common changes, not ADC-local. §20 is held at implementation 9 by decision over a proposed 8 on inline styles: the two Selection dashboards carry zero inline styles, their values riding semantic classes in the module stylesheet (`MMCA.ADC/Source/Modules/Conference/MMCA.ADC.Conference.UI/wwwroot/adc-ui.css:71` `.text-center-cell` through `:99` `.flex-fill-1`, global because they land on markup MudBlazor renders), and the repo-wide count is 42 across 26 `.razor` files (largest holders `SessionSelectionSpeakerOverlap.razor` and `PublicSpeakerDetail.razor` at 4 each). **Re-open trigger:** the count growing past 42 across 26 files, or a further shared-class extraction the next re-score can credit.
- **Deploy E2E gate is chromium-only, ui-scoped and cross-engine by recency:** `e2e-gate` invokes one browser (`deploy.yml:788`, `browsers: '["chromium"]'` at `:802`) and runs only on UI-affecting diffs (paired with **TD-20**); firefox and webkit run on alternating weekly single-engine nightly legs (Monday firefox, Thursday webkit) with `continue-on-error` on the schedule (`e2e.yml:147`). `cross-browser-freshness` (`deploy.yml:1034`, `:1305`, `:1347`) requires an in-window green proof per engine, so cross-engine coverage is enforced by recency rather than per-deploy execution: a UI deploy can ship code never run on firefox or webkit until the next weekly leg goes red and stale. `backend-test-gate` (`deploy.yml:394-396`) runs the CI.slnf unit, architecture and bUnit tier on the deploys `e2e-gate` skips. A deliberate Actions-minute trade-off.
- **E2E runs Blazor Server mode in CI:** `e2e.yml` pins `E2E_FORCE_SERVER=true`; forcing WebAssembly in CI fails wholesale on the 2-core hosted runner (every fresh browser context pays a cold WASM boot and the readiness probe is satisfied during prerender). `E2E_FORCE_WASM` stays supported for local runs. Retrying WASM in CI needs all three of: a WASM-aware readiness signal, amortized per-context WASM boot, and a raised job cap. A faster self-hosted runner makes the suite fail more (Blazor Server circuits drop under sustained fast-suite load), so do not repeat the rejected test-side and circuit-config tweaks; the only paths that work are a slower pace or dedicated UI-host CPU.
- **Freshness-gate break-glass:** the recency gates (`dr-freshness`, `load-freshness`, `cross-service-freshness`) each accept a `skip_freshness_gates` workflow_dispatch input with a required justification (declared at `deploy.yml:13-18`, per-gate checks at `:562`, `:619`, `:678`), so each proof is bypassable by an operator. This qualifies the "enforced deploy precondition" language used for §6, §12, §29 and §33.
- **Pseudo-localization breadth deferred (§27):** broadening `PseudoLocalizationTests` beyond its three public pages (`PseudoLocalizationTests.cs:51`, public by design per `:31`) across the authenticated authoring surface is worth 1 weighted point and costs authenticated-login plumbing plus text-expansion and overflow assertions across roughly 54 of the 57 routable `@page` files, the weakest cost-to-benefit ratio on either band. The assertion is a rendered-layout property, so bUnit cannot close it, and every browser case added to the deploy-gating chromium `e2e-gate` is paid on every future deploy. A citable culture-aware-formatting violation also stands against the lift. §27 keeps its band row at implPriority 1, but the lever is not to be re-proposed without new evidence. **Re-open triggers:** a second locale beyond `es`, any RTL locale, or a reported layout regression on an authenticated page. Maturity 4 is unaffected (`TranslationCompletenessTests` + `LocalizedTextConventionTests`, both in `MMCA.ADC.CI.slnf:58`, run at `deploy.yml:284`).
- **BR-130 room double-booking overlap check is a SOFT guard:** `SessionRoomScheduling.ValidateRoomAssignmentAsync` is a read-then-write advisory check with no transaction, lock or DB exclusion constraint, so two concurrent organizer writes for the same room with overlapping windows can both pass. Each alternative is rejected on evidence: a transactional re-check cannot close the race below SERIALIZABLE (which escalates to key-range or table locks across the Sessionize import path with no `(RoomId, StartsAt)` index); `IDistributedLock`'s contract forbids sole-guard use on a correctness invariant and degrades to in-process when Redis is absent; `sp_getapplock` needs an EF/SqlClient reference the Application layer forbids. The spec's BR-130 promises only the cross-event room check (422); a double-booking is repairable by editing either session. Documented in the class XML doc (ADC PR #94). **Re-open triggers:** organizer concurrency materially above today's handful, a real double-booking incident, or a cheap DB-level range-exclusion capability.
- **§16 is scored, not N/A:** rubric v2's "N/A in all three repos" claim ([ADR-110](../adr/110-rubric-v2-category-realignment.md)) does not hold for ADC, because AI session scoring calls a model in production. §16 sits at M4/I10, governed by [ADR-111](../adr/111-ai-session-scoring-governance.md) and [ADR-120](../adr/120-governed-chat-client-boundary.md). The ADR-110 wording is a Website-side correction for `/update-adrs`. The former Maintainability & Evolvability criteria score under §34 (coupling, tech-debt register), §32 (lockstep upgrades) and §33 (onboarding).
- [ ] **SQL data plane on public network access, password path retained (§11 / §17):** `publicNetworkAccess: 'Enabled'` (`infra/main.bicep:764`) plus the AllowAzureServices 0.0.0.0 firewall rule (`:768-775`), with `azureADOnlyAuthentication` deliberately not set (`:779`, the password path kept as a dormant fallback) and the `sqlAdminPassword` parameter always supplied (`deploy.yml:1444`); managed-identity SQL auth is opt-in through the `useManagedIdentitySql` repo variable (bicep default `false`, `:36`) and active in production. Moving onto private endpoints is the VNet and private-endpoint epic, which recreates the Container Apps environment, so it is deferred by design and documented-accepted in `infra/SQL-MANAGED-IDENTITY.md`; it is the 9→10 rung, not a scheduled lever. The two `AllowAnonymous` internal gRPC surfaces (`Engagement.Service/Program.cs:341`, `Notification.Service/Program.cs:290`) are accepted with their compensating controls: bounded payloads, no user ids on the wire, no Gateway route. **Re-open triggers:** a VNet-integrated environment landing for another reason, or a data-plane exposure finding.
- [ ] **§11 threat model not scheduled (TD-26, effort M):** a committed STRIDE-style model per trust boundary (Gateway edge, per-service gRPC, outbox/broker, Key Vault, mobile client) under `infra/` or `Docs/` is cut by decision; do not re-schedule it without asking. The rubric's "no threat model" red flag stays citable (no threat-model file exists in the repo), so §11 holds at implementation 8; the mitigations such a model would enumerate are enforced individually (the CI-gated `Tests/Architecture/MMCA.ADC.Architecture.Tests/Api/AnonymousEndpointTests.cs:21` allowlist, the gating supply-chain job, the compensating controls above). **Re-open triggers:** an explicit request for the model, a compliance requirement that names one, or a security finding a maintained model would have caught.
- [ ] **TD-15 (effort L) · topology collapse to one host and one database:** the framework supports collapsing the distributed topology into a modular monolith with no application-code rewrite (`AddBrokerMessaging` falls back to the in-process bus, `DataSourceResolver` collapses the per-module sources onto one database, `ModuleLoader` boots all four modules in one host; MMCA.Helpdesk is the single-host proof). The recorded estimate is a run cost of ~$190-220/mo cut to roughly a third, with the gRPC partial-failure class eliminated (ACA ~$110-130, SQL ~$25, Log Analytics ~$25, Redis ~$13 at Balanced B0, Service Bus ~$10 at Standard); those dollar figures are unverified against billing. **Blocker (deliberate):** the distributed topology is the GTM demonstrator of the framework's extract-a-service path. **Resolution path when revisited:** single service host enabling all modules (Helpdesk pattern), one `ADC` database via the resolver collapse, drop Service Bus/Redis/Gateway from `main.bicep`, point the UI at the host, and re-run the k6 capacity proof at the collapsed tier.
- [ ] **Batch the feedback submit:** `EventFeedback`/`SessionFeedback` send one HTTP POST per answered question over a 10-question set, each its own transaction, outbox write and audit stamp. Needs a batch upsert command and endpoint. Recorded, not scheduled.
- [ ] **Session-scoped live-poll management endpoint:** `SessionLive` fetches event-wide polls and filters client-side. Not a type-compatible swap (`GetEventPollsAsync` returns `LivePollDTO`, the session-scoped call `LivePollResultsDTO`), and the event list is organizer-only with speakers falling back on 403, so it needs an authorization decision too. Recorded, not scheduled.
- [ ] **`GetOpenPollsHandler` issues two queries per poll:** the N+1 shape `SessionQuestionViewBuilder` removed, one file over. Theoretical at ADC's scale (1-3 open polls), so recorded rather than fixed.
- **UI read cache not adopted (§12):** the Conference UI services do not opt into `IUiReadCache` (zero occurrences in ADC `Source`); the opt-in caused e2e-gate staleness failures. This is the §12 9→10 residual.
- [~] **Optional cross-service broker-tier coverage (§14):** the Testcontainers RabbitMQ + SQL round-trip tier (`MMCA.ADC.CrossService.IntegrationTests`, nightly `cross-service-tests.yml`, its recency gating deploys through `cross-service-freshness`) does not cover speaker analytics or the Conference→Engagement bookmark-count gRPC reads. Optional, not scheduled.
- **Dedicated rate-limit integration fixtures skipped:** the WebApplicationFactory hosts neutralize the limiter; proving the 300/min cap and the per-IP registration throttle needs a tight-limit fixture variant, low value at this volume. Revisit only if abuse is observed.
- **Refresh cookie keeps `SameSite=Lax`, not `Strict`:** Lax is load-bearing for the SSR-prerender path (`[Authorize]` pages opened in a new tab, on F5 or from an external link are cross-site top-level navigations that Strict would strip the cookie from, forcing a spurious /login bounce, the scenario ADR-022's cookie scheme exists to serve). CSRF is covered by the `/auth/session/token` endpoint's `Sec-Fetch-Site` check, POST-only, and `SameSite=Lax`. The session/refresh cookie lives 7 days (`SessionCookieJar`, aligned to the refresh-token lifetime).
- **Data export excludes feedback answers (§30):** the export aggregates Identity, Engagement (bookmarks, submitted questions) and Notification (inbox items); event and session feedback answers live in the Conference database and would need a third gRPC edge. Out of scope by decision.

## Resolved

| # | Category | M / I | Evidence |
|---|---|---|---|
| §1 | SOLID Principles | 4 / 9 | Constructor ceilings gate CI (`Tests/Architecture/MMCA.ADC.Architecture.Tests/Cqrs/ConstructorDependencyCountTests.cs:52,:61`); four oversized controllers split by sub-resource with no URL, verb or authorization change (`Conference.API/Controllers/Events/EventLifecycleController.cs:32`, `Speakers/SpeakerLinksController.cs:33`, `Sessions/SessionCalendarController.cs:26`, `Engagement.API/Controllers/LivePollVotingController.cs:35`) |
| §2 | Design Patterns | 4 / 9 | Result-returning factories and per-entity sync strategies (`Identity.Domain/Users/User.cs:158`; five `*SyncStrategy.cs`) |
| §3 | Clean Architecture | 4 / 9 | Layer, purity and transport-at-edge rules in the CI arch gate (`MMCA.ADC.CI.slnf:58`); `MicroserviceExtractionTests` enforce ADR-007 |
| §4 | Domain-Driven Design | 4 / 9 | `Email` value object inside `Event` (`Event.cs:59`, invariant at `:189`, `:265`); no public setters in Conference.Domain; `EntityConventionTests.cs:3` |
| §5 | Vertical Slice Architecture (maturity) | 4 / 8 | Slice cohesion is a CI merge gate (`SliceCohesionTests.cs:8`); TD-19: the enforced map covers all four modules (`AdcArchitectureMap.cs:51-54`) |
| §6 | CQRS & Event-Driven | 4 / 9 | Frozen integration-event contract (`IntegrationEventContractTests.cs:9-14`); TD-02: `EnableInbox=true` on all four services and the broker round-trip gating deploys through `cross-service-freshness` |
| §8 | Data Architecture | 4 / 9 | TD-03 concurrency round-trip (`UpdateEventHandler.cs:34`, `OrganizerConcurrencyTests.cs:44`); TD-04 outbox fidelity (`OutboxFidelityTests`); model-drift gate (`deploy.yml:384`); `SoftDeleteFidelityTests` |
| §9 | API & Contract Design (maturity) | 4 / 8 | OpenAPI per service at `/openapi/v1.json`; v1.0 deprecated / v2.0 pair (`ServiceInfoController.cs:18-19`, `ApiVersioningTests.cs:14`); extraction ADR-008 |
| §11 | Security (maturity) | 4 / 8 | Global rate limiter (MMCA.Common `AddCommonRateLimiting`, 300 req/min per authenticated user); authz access-denied matrices gating every PR; all runtime secrets in RBAC Key Vault via the shared UAMI; `AnonymousEndpointTests.cs:21` |
| §11 / §12 | UI host hardening | 4 / 8, 3 / 9 | The hardening kit consumed from `MMCA.Common.UI.Web.Hardening` (`Source/Hosts/UI/MMCA.ADC.UI.Web/Program.cs:31`), host wiring pinned by `Tests/Hosts/MMCA.ADC.UI.Web.Tests/UiRateLimitingTests.cs:29` (ADR-088) |
| §12 | Performance & Scalability (implementation) | 3 / 9 | TD-21: k6 green through the synthetic-traffic bypass; hub fan-out at `maxReplicas: 2` (`TwoReplicaHubFanOutTests.cs:49`); backpressure on all four services (`Conference.Service/appsettings.json:50`); Redis-backed output cache across replicas (`Conference.Service/Program.cs:176`) |
| §12 | Performance program | 3 / 9 | Junction/lookup reads cached with parent-tag eviction; scoring evicts `conference:sessions` only; bookmark counts on a 60s policy; feedback answers out of public payloads; speaker session lists filtered server-side; `LivePoll(SessionId, Status)` indexed; `conference-read-load.js:56-57` exercises the `includeChildren=true` shapes |
| §13 | Observability & Operability | 4 / 9 | Alert-to-runbook pairing gate (`ObservabilityConventionTests.cs`, `MMCA.ADC.CI.slnf:58`); SLO workbook (`infra/main.bicep:508`); per-alert runbook (`infra/OPERATIONS.md:13`) |
| §14 | Testability & Test Strategy | 4 / 9 | Per-service WebApplicationFactory integration tiers gating every PR; TD-05 coverage floor 55.5% (`deploy.yml:319`); cross-service handler tests (`CrossServiceUserRegisteredTests.cs`, `CrossServiceSpeakerLinkTests.cs`) |
| §15 | Best Practices & Code Quality (hygiene) | 4 / 8 | Zero `NuGetAuditSuppress` items; the patched SSH.NET pin (`Directory.Packages.props:82`); every global NoWarn code dated and justified (`Directory.Build.props:16-31`) |
| §16 | AI-Native Application Architecture | 4 / 10 | TD-22 golden replay and prompt hash (`GoldenReplayTests.cs:183`, `PromptContractTests.cs:57`); TD-23 input envelope and redaction; TD-25 trace source (`MMCA.Common.Aspire/Extensions.cs:185`); output guardrail at the boundary (`SessionScoreResponseGuardrail.cs:74`, pinned by `ConferenceAiGuardrailsRegistrationTests.cs:81`); permanent kill switch (`ConferenceFeatures.cs:38`) |
| §17 | DevOps & Deployment | 4 / 9 | Passwordless managed-identity SQL auth in production; ACR pulled via UAMI with the admin user disabled; post-deploy smoke plus auto-rollback (`deploy.yml:1099-1174`) |
| §18 | UI Architecture & Components (maturity) | 4 / 8 | UI-architecture fitness gate (`UIArchitectureConventionTests.cs:12`); TD-13 splits of `SessionSelectionDashboard` and `SpeakerDetail` |
| §19 | State Management & Data Flow | 4 / 9 | State-management fitness gate (`StateManagementConventionTests.cs:11`); live-accessor `UnsavedChangesGuard` on all create and detail forms (`UnsavedChangesGuard.razor:32`) |
| §20 | Design System & UI Consistency | 4 / 9 | Brand-token guard (`Ui/BrandColorTokenTests.cs:12`); one shared `ADCHome.razor.css` on `var(--mmca-primary)` (`:249`); Secondary family guarded Common-side (`MMCA.Common.UI.Tests/Theme/BrandColorTokenTests.cs:38,:60`) |
| §21 | Accessibility (implementation) | 3 / 8 | axe inside the deploy-gating `e2e-gate` (`AccessibilityTests.cs:23`); outlined Warning surfaces moved to AA-passing variants on six sites |
| §22 | Responsive & Cross-Browser (maturity) | 4 / 7 | Per-engine `cross-browser-freshness` deploy gate (`deploy.yml:1034`, `:1305`, `:1347`); documented support matrix |
| §23 | Front-End Performance (maturity) | 4 / 8 | Core Web Vitals budgets asserted inside the deploy-gating `e2e-gate` (`WebVitalsTests.cs:39`) |
| §24 | Forms, Validation & UX Safety (maturity) | 4 / 8 | `FormsConventionTests.cs:18,32`; TD-14: `<ErrorSummary>` on 21 of 21 `MudForm` files (`EventCreate.razor:41`) with a render-level bUnit assertion |
| §25 | Navigation & Information Arch | 4 / 8 | Route-authorization gates (`ManagementRouteAuthorizationTests.cs:19`, `IdentityRouteAuthorizationTests.cs:35`); activity, QR, speaker-QR and role-administration routes documented (`adc-NavigationFlow.md:576,:629,:633`) |
| §26 | Front-End Security | 4 / 9 | Cookie-only refresh and in-memory access token; single-use OAuth code exchange (`OAuthController.ExchangeAsync`); enforced CSP and hardened headers on UI host and Gateway (`Source/Hosts/MMCA.ADC.Gateway/Program.cs:31,61`); TD-09 `SecurityHeadersTests` |
| §27 | Internationalization (i18n) | 4 / 8 | en-US + es with 68 resx pairs; `TranslationCompletenessTests.cs:12` and `LocalizedTextConventionTests.cs` in CI; pseudo-localization tier on the public pages (`PseudoLocalizationTests.cs:51-56`) |
| §28 | Front-End Testing & Quality | 4 / 9 | TD-06/TD-07: chromium E2E/axe suite as the deploy gate; markup-snapshot tier (`ComponentsSnapshotTests.cs:27`); `backend-test-gate` (`deploy.yml:394-396`); `ManagementRouteAuthorizationTests` |
| §29 | Resilience & Business Continuity | 4 / 9 | TD-10: drilled PITR restores (`DISASTER-RECOVERY.md:153-158`); `dr-freshness` deploy gate (`deploy.yml:699`); LTR on all four DBs (`infra/main.bicep:668`); `GracefulShutdownTests` |
| §30 | Compliance, Privacy & Governance | 4 / 9 | Anonymize-in-place erasure (`Identity.Domain/Users/User.cs:465`, `DeleteUserHandler.cs:47-49`); PII-free logging and erasure proven by `ErasureAndPiiLoggingTests`; export aggregated across Identity, Engagement and Notification; `DataResidencyTests` |
| §31 | Cost Efficiency / FinOps (maturity) | 4 / 8 | `cost-guard` in `deploy.needs` (`deploy.yml:776`); monthly RG budget (`infra/main.bicep:732`); `commonTags` on every billable resource |
| §32 | Dependency & Supply-Chain | 4 / 9 | TD-01: 67 committed lock files restored `--locked-mode` (`deploy.yml:264`); blocking audit and SBOM gates (`:456-476`); `FrameworkVersionConsistencyTests` |
| §33 | Developer Experience & Inner Loop | 4 / 9 | TD-17: authoritative Service Bus emulator tier (`cross-service-tests.yml:153`) required by `cross-service-freshness` (`deploy.yml:874`); `ADC_BROKER=servicebus` local profile (`AppHost/Program.cs:91`); full getting-started README |
| §34 | Architecture Governance & Docs | 4 / 9 | ADRs 001-125 (`../adr/README.md:6`); 44 CI-gated architecture-test classes; this ledger carries every `TD-NN` item |
| A-1..A-7 | Defect audit | - | Cancellation propagates past scoring catches; partial score JSON rejected; duplicate category-item guards; case-insensitive `UserRole.IsOrganizer(string?)` in `DeleteUserHandler`/`ExportUserDataHandler`; category-distribution soft-delete predicate aligned |
| - | Code health | - | BR-49 eligibility in one place (`SessionStatuses` in Conference.Shared); 15 read-only query handlers on `GetReadRepository`; calendar exports on the injected clock; `ADCHome` on the UI services; shared test builders and `MMCA.Common.Testing` fakes; `LatestLoadGuard` on five detail pages |
